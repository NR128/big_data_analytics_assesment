# 📙 빅데이터분석기사 필기 — 3과목 오답노트 (빅데이터 모델링)

> **3과목(빅데이터 모델링) 200제를 자동 채점 스프레드시트로 전부 풀고, 틀리거나 헷갈린 108문항을 개념별로 정리했습니다.** 1·2과목 오답노트와 같은 방식으로 회차 구분 없이 개념 단위로 구성합니다.
>
> **출처**: 업로드하신 `정답리스트` 채점 스프레드시트(문항·정답·내 답·헷갈림·채점 O/X·해설이 이미 기록되어 있었습니다)와 `빅분기_3과목_예상문제_200제.md`(문제·보기 원문)를 대조해 작성했습니다.
> **표기**: ❌ 틀림 · ⚠️ 찍어서 맞음/헷갈림 · ✅ 졸업
> **복습 주기**: D+1 / D+3 / D+7 세 번 다시 봅니다. 세 번 다 맞히면 ✅ 표시 후 졸업.
> **범위**: 3과목 공식 출제범위(분석 모형 설계 / 분석 기법 적용 / 고급 분석 기법) 순서 대신, 스프레드시트에 이미 정리되어 있던 **13개 세부 영역**(분석모형 설계·검증 → 회귀분석 → 로지스틱·분류 평가지표 → 의사결정나무 → 인공신경망 → SVM·KNN·나이브베이즈 → 연관분석 → 군집분석 → 앙상블 → 딥러닝 → 시계열 분석 → 다변량·비정형 분석 → 종합) 순서를 그대로 따랐습니다.

---

## 🔍 전문가 검토 결과

1·2과목 오답노트와 동일한 절차로 108문항의 정답·핵심 정의를 재검토했습니다. (실제 외부 인력이 아니라 5가지 관점으로 나누어 스스로 재점검한 절차입니다.)

| 관점 | 중점 점검 사항 | 결과 |
|---|---|---|
| **출제위원 시각** | 스프레드시트의 정답·해설이 문제 원문 및 표준 교재와 일치하는가 | 108문항 전수 확인, **불일치 없음** |
| **학원강사 시각** | CART·C4.5·CHAID, 배깅·부스팅, AR·MA 등 구분 키워드가 정확한가 | 개념 표 13개로 재구성 |
| **데이터 분석 전문가 시각** | 혼동행렬 공식, 정상성 조건, 앙상블 구조 설명이 실무 정의와 어긋나지 않는가 | 뉴앙스 주석 2건 추가(아래 참고) |
| **교수 시각** | 판별분석↔군집분석, AR↔MA 등 개념 간 경계의 논리적 정합성 | 정합성 문제 없음 |
| **현직 데이터 사이언티스트 시각** | XGBoost·Adam·배치정규화 등 최신 기법 설명이 실무 관행과 부합하는가 | 실무 단순화 주석 1건 |

**뉴앙스 주석**
- **VIF ≥ 10을 다중공선성 기준으로 쓰는 것**은 표준 교재 기준입니다. 실무에서는 **더 엄격하게 5를 기준으로 쓰는 경우도 흔합니다**(원본 스프레드시트 해설에도 "엄격하게는 5"로 병기되어 있어 그대로 유지했습니다).
- **"랜덤포레스트=배깅+무작위 변수 선택"**이라는 설명은 정확하지만, 실무에서는 **변수뿐 아니라 분리 시점마다 후보 변수 집합 자체를 무작위로 재선택**한다는 점까지 알아두면 좋습니다. 시험 답안 기준으로는 현재 설명으로 충분합니다.
- **딥러닝 최적화 기법(Adam 등)**은 실무에서 계속 발전하고 있는 영역이라, 시험 범위(Adam=모멘텀+RMSProp)를 벗어난 최신 옵티마이저는 별도 학습이 필요할 수 있습니다.

**결론 — 108문항의 정답·핵심 정의에서 사실 오류는 발견되지 않았습니다.**

---

## 📊 전체 현황

**200제 완주 — 오답 48 · 헷갈림 60 · 정답률 76%(152/200)** — 헷갈린 60문항까지 실전 오답으로 가정하면 **44%**로, 1·2과목에서 언급한 합격 커트라인(60%)에 크게 못 미치고 **과목별 최저 기준(40%)에도 위험할 수 있는 수준**입니다. 3과목은 세 과목 중 가장 시급한 보완이 필요합니다.

> 참고: 59번과 199번은 채점 스프레드시트에 오답이면서 동시에 헷갈림으로도 표시되어 있어, 더 심각한 쪽인 **오답으로만** 집계했습니다(아래 표의 헷갈림 수치에는 반영하지 않음).

| 영역 | 문항 수 | 오답 | 헷갈림 | 플래그율 | 상태 |
|---|---|---|---|---|---|
| 분석모형 설계·검증 | 20 | 6 | 3 | 45% | 🟡 보완 |
| 회귀분석 | 20 | 7 | 3 | 50% | 🟡 보완 |
| 로지스틱·분류 평가지표 | 23 | 3 | 8 | 48% | 🟡 보완 |
| 의사결정나무 | 13 | 4 | 3 | 54% | 🟡 보완 |
| 인공신경망 | 19 | 3 | 6 | 47% | 🟡 보완 |
| SVM·KNN·나이브베이즈 | 10 | 2 | 1 | 30% | 🟢 양호 |
| 연관분석 | 9 | 5 | 0 | 56% | 🔴 최우선 |
| 군집분석 | 21 | 2 | 10 | 57% | 🔴 최우선 |
| 앙상블 | 12 | 2 | 6 | 67% | 🔴 최우선 |
| 딥러닝 | 12 | 3 | 2 | 42% | 🟡 보완 |
| **시계열 분석** | 13 | 6 | 6 | **92%** | 🔴 **최위험** |
| 다변량·비정형 분석 | 18 | 4 | 7 | 61% | 🔴 최우선 |
| 종합 | 10 | 1 | 5 | 60% | 🔴 최우선 |

**🔴 최우선 보완 구간 — 시계열 분석(92%)** — 13문항 중 **12문항**이 오답·헷갈림입니다. 3과목 전체에서 가장 취약한 단일 영역이며, 정상성·AR/MA/ARIMA·ACF/PACF라는 큰 그림 자체가 서 있지 않은 상태로 보입니다. 아래 해당 섹션의 총정리표부터 확인하세요.

**그 다음 우선순위** — 앙상블(67%), 종합(70%), 다변량·비정형(61%), 군집분석(57%), 연관분석(56%) 순으로 플래그율이 높습니다. 연관분석은 헷갈림 없이 **전부 확정 오답(5/5)**이라 공식 자체를 새로 암기해야 하는 영역이고, 군집분석은 **헷갈림이 10개**로 확신도가 가장 낮은 영역입니다.

---

## PART 1. 3과목 전 영역 (분석 모형 설계 · 기법 적용 · 고급 분석)

#### 1-1. 분석 모형 설계 · 검증

### ⚠️ 4번 — 다음 중 지도학습 기법에 해당하지 않는 것은? (헷갈림)

**Q.** 다음 중 지도학습 기법에 해당하지 않는 것은? → **정답 ④ K-평균 군집**

**해설** — K-평균 군집은 비지도학습.

---

### ❌ 5번 — 종속변수가 범주형일 때 사용하는 분석 기법은?

**Q.** 종속변수가 범주형일 때 사용하는 분석 기법은?
① 분류분석 ② 회귀분석 ③ 군집분석 ④ 연관분석

| 내 답 | 정답 |
|---|---|
| ③ 군집분석 | **① 분류분석** |

**해설** — 종속변수가 범주형이면 분류, 연속형이면 회귀.

---

### ❌ 7번 — 검증용 데이터(Validation Set)의 역할로 가장 적절한 것은?

**Q.** 검증용 데이터(Validation Set)의 역할로 가장 적절한 것은?
① 모델의 파라미터를 학습시킨다 ② 최종 성능을 보고하는 데만 사용한다 ③ 하이퍼파라미터 조정과 모델 선택에 사용한다 ④ 데이터를 정제하는 데 사용한다

| 내 답 | 정답 |
|---|---|
| ② 최종 성능을 보고하는 데만 사용한다 | **③ 하이퍼파라미터 조정과 모델 선택에 사용한다** |

**해설** — 검증용은 하이퍼파라미터 튜닝·모델 선택용. 최종 성능 보고는 평가용(Test)이 담당.

---

### ❌ 8번 — 데이터를 학습용과 평가용으로 한 번만 나누는 가장 단순한 검증 방법은?

**Q.** 데이터를 학습용과 평가용으로 한 번만 나누는 가장 단순한 검증 방법은?
① 부트스트랩 ② K-fold 교차검증 ③ LOOCV ④ 홀드아웃(Hold-out)

| 내 답 | 정답 |
|---|---|
| ① 부트스트랩 | **④ 홀드아웃(Hold-out)** |

**해설** — 홀드아웃 = 한 번만 분할하는 가장 단순한 방식.

---

### ❌ 9번 — 홀드아웃 방법의 단점으로 옳은 것은?

**Q.** 홀드아웃 방법의 단점으로 옳은 것은?
① 데이터를 어떻게 나누느냐에 따라 결과의 변동이 크다 ② 계산량이 매우 많다 ③ 과적합을 전혀 탐지할 수 없다 ④ 대용량 데이터에만 적용 가능하다

| 내 답 | 정답 |
|---|---|
| ④ 대용량 데이터에만 적용 가능하다 | **① 데이터를 어떻게 나누느냐에 따라 결과의 변동이 크다** |

**해설** — 분할 방식에 따라 성능 추정치의 변동(분산)이 크다.

---

### ❌ 11번 — 관측치가 n개일 때 K=n으로 설정한 교차검증 방법은?

**Q.** 관측치가 n개일 때 K=n으로 설정한 교차검증 방법은?
① 2-fold ② 홀드아웃 ③ LOOCV(Leave-One-Out) ④ 층화 K-fold

| 내 답 | 정답 |
|---|---|
| ① 2-fold | **③ LOOCV(Leave-One-Out)** |

**해설** — LOOCV = K를 관측치 수 n으로 설정한 극단적 교차검증.

---

### ⚠️ 12번 — 복원추출을 반복하여 여러 개의 표본을 생성해 모델을 평가하는 방법은? (헷갈림)

**Q.** 복원추출을 반복하여 여러 개의 표본을 생성해 모델을 평가하는 방법은? → **정답 ④ 부트스트랩**

**해설** — 부트스트랩 = 복원추출 반복으로 표본 생성.

---

### ⚠️ 16번 — 편향(Bias)과 분산(Variance)에 대한 설명으로 옳은 것은? (헷갈림)

**Q.** 편향(Bias)과 분산(Variance)에 대한 설명으로 옳은 것은? → **정답 ④ 모델이 복잡할수록 편향은 작아지고 분산은 커진다**

**해설** — 복잡한 모델일수록 편향↓·분산↑ (편향-분산 트레이드오프).

---

### ❌ 17번 — 과적합 상태의 모델이 갖는 특징으로 옳은 것은?

**Q.** 과적합 상태의 모델이 갖는 특징으로 옳은 것은?
① 편향 낮음, 분산 높음 ② 편향 높음, 분산 낮음 ③ 편향·분산 모두 높음 ④ 편향·분산 모두 낮음

| 내 답 | 정답 |
|---|---|
| ② 편향 높음, 분산 낮음 | **① 편향 낮음, 분산 높음** |

**해설** — 과적합 = 낮은 편향 + 높은 분산. 과소적합은 그 반대.

---

> ⚠️ **20문항 중 9문항(45%)이 오답·헷갈림입니다.** 데이터 분할·교차검증 방법(홀드아웃/K-fold/LOOCV/부트스트랩)이 서로 섞여 있고, 검증용·평가용 데이터의 역할 구분도 흔들립니다.

**개념 정리 — 데이터 분할과 교차검증 총정리 (5·7~11번 동시 해결)**

| 세트 | 역할 |
|---|---|
| **학습용(Train)** | 모델을 **학습**시키는 데이터 |
| **검증용(Validation)** | **하이퍼파라미터 튜닝·모델 선택**에 사용 (최종 성능 보고 X) |
| **평가용(Test)** | **최종 성능**을 객관적으로 보고 |

| 교차검증 기법 | 방법 | 특징 |
|---|---|---|
| **홀드아웃** | 학습·평가용으로 **한 번만** 분할 | 가장 단순, 분할 방식에 따라 **성능 추정치 분산이 큼** |
| **K-fold** | K개 폴드로 나눠 **각각 한 번씩** 평가용으로 사용 | 홀드아웃보다 안정적 |
| **LOOCV** | **K = 관측치 수 n** (한 개씩만 평가용) | 가장 극단적인 K-fold, 계산량 많음 |
| **부트스트랩** | **복원추출**을 반복해 표본 생성 | 같은 데이터가 중복 선택될 수 있음 |

**암기팁** — **"검증용=연습 시험(과외선생 피드백용), 평가용=진짜 수능(성적표에 찍힘)"** — 검증용 데이터로 아무리 잘 나와도 그건 "최종 성적"이 아니라 "튜닝 참고용"입니다.

**실수 포인트 ⚠️** — 5번(종속변수 범주형→분류)은 회귀분석과 정확히 반대 조건입니다: **연속형=회귀, 범주형=분류**. 17번(과적합=낮은 편향+높은 분산)은 반대로 답하기 쉬운데, **과적합은 "학습 데이터에 너무 딱 맞춰서" 편향은 낮고(학습 데이터는 잘 맞힘) 분산은 높습니다(새 데이터에서 흔들림)**. 16번(복잡한 모델→편향↓·분산↑)과 세트로 기억하세요 — **모델이 복잡해질수록 과적합 방향(편향↓분산↑)으로 이동**합니다.

---

#### 1-2. 회귀분석

### ⚠️ 22번 — 회귀계수를 추정할 때 잔차 제곱합을 최소화하는 방법은? (헷갈림)

**Q.** 회귀계수를 추정할 때 잔차 제곱합을 최소화하는 방법은? → **정답 ② 최소제곱법**

**해설** — 잔차제곱합 최소화 = 최소제곱법(OLS).

---

### ❌ 25번 — 회귀분석에서 정규성 가정의 대상은 무엇인가?

**Q.** 회귀분석에서 정규성 가정의 대상은 무엇인가?
① 오차항(잔차) ② 종속변수의 원자료 ③ 독립변수 ④ 회귀계수

| 내 답 | 정답 |
|---|---|
| ② 종속변수의 원자료 | **① 오차항(잔차)** |

**해설** — 회귀분석의 정규성 가정 대상은 오차항(잔차).

---

### ⚠️ 26번 — 잔차 그래프에서 부채꼴 모양으로 퍼지는 형태가 나타났다면 위배된 가정은? (헷갈림)

**Q.** 잔차 그래프에서 부채꼴 모양으로 퍼지는 형태가 나타났다면 위배된 가정은? → **정답 ② 등분산성**

**해설** — 잔차가 부채꼴로 퍼지면 등분산성 위배(이분산성).

---

### ❌ 27번 — 오차의 독립성(자기상관 유무)을 진단하는 통계량은?

**Q.** 오차의 독립성(자기상관 유무)을 진단하는 통계량은?
① Cook's Distance ② VIF ③ 더빈-왓슨(Durbin-Watson) ④ F 통계량

| 내 답 | 정답 |
|---|---|
| ② VIF | **③ 더빈-왓슨(Durbin-Watson)** |

**해설** — 더빈-왓슨 통계량으로 자기상관(독립성) 진단.

---

### ❌ 28번 — 더빈-왓슨 통계량 값이 2에 가까울 때의 해석으로 옳은 것은?

**Q.** 더빈-왓슨 통계량 값이 2에 가까울 때의 해석으로 옳은 것은?
① 양의 자기상관이 있다 ② 음의 자기상관이 있다 ③ 등분산성이 위배되었다 ④ 자기상관이 없다

| 내 답 | 정답 |
|---|---|
| ① 양의 자기상관이 있다 | **④ 자기상관이 없다** |

**해설** — DW ≈ 2 → 자기상관 없음. 0에 가까우면 양의, 4에 가까우면 음의 자기상관.

---

### ❌ 32번 — 다중회귀분석에서 회귀모형 전체의 유의성을 검정하는 통계량은?

**Q.** 다중회귀분석에서 회귀모형 전체의 유의성을 검정하는 통계량은?
① t 통계량 ② Z 통계량 ③ 카이제곱 통계량 ④ F 통계량

| 내 답 | 정답 |
|---|---|
| ③ 카이제곱 통계량 | **④ F 통계량** |

**해설** — 모형 전체(모든 회귀계수 = 0)의 유의성은 F검정.

---

### ❌ 33번 — 다중회귀분석에서 개별 회귀계수의 유의성을 검정하는 통계량은?

**Q.** 다중회귀분석에서 개별 회귀계수의 유의성을 검정하는 통계량은?
① t 통계량 ② F 통계량 ③ 더빈-왓슨 ④ VIF

| 내 답 | 정답 |
|---|---|
| ② F 통계량 | **① t 통계량** |

**해설** — 개별 회귀계수의 유의성은 t검정.

---

### ❌ 35번 — 다중공선성 진단 지표인 VIF 값이 일반적으로 얼마 이상일 때 문제로 판단하는가?

**Q.** 다중공선성 진단 지표인 VIF 값이 일반적으로 얼마 이상일 때 문제로 판단하는가?
① 0.5 ② 1 ③ 3 ④ 10

| 내 답 | 정답 |
|---|---|
| ② 1 | **④ 10** |

**해설** — 일반적으로 VIF ≥ 10(엄격하게는 5)이면 다중공선성 의심.

---

### ❌ 37번 — 모형의 적합도와 복잡도를 함께 고려하여 값이 작을수록 좋은 모형으로 판단하는 지표는?

**Q.** 모형의 적합도와 복잡도를 함께 고려하여 값이 작을수록 좋은 모형으로 판단하는 지표는?
① AIC ② R² ③ F 통계량 ④ 상관계수

| 내 답 | 정답 |
|---|---|
| ② R² | **① AIC** |

**해설** — AIC는 값이 작을수록 좋은 모형.

---

### ⚠️ 38번 — AIC와 BIC의 차이로 옳은 것은? (헷갈림)

**Q.** AIC와 BIC의 차이로 옳은 것은? → **정답 ② BIC가 표본 크기를 반영해 복잡한 모형에 더 강한 벌점을 부과한다**

**해설** — BIC는 log(n)을 벌점에 반영해 복잡한 모형에 더 강한 페널티를 준다.

---

> ⚠️ **20문항 중 10문항(50%)이 오답·헷갈림입니다.** 회귀분석의 4대 가정, 회귀 진단 통계량(DW·VIF·AIC/BIC), F검정·t검정의 역할이 섞여 있습니다.

**개념 정리 — 회귀분석 가정과 진단 총정리 (25·27·28·32·33·35·37·38번 동시 해결)**

| 가정 | 진단 방법 | 위배 시 |
|---|---|---|
| **선형성** | 잔차 산점도 | 비선형 관계 |
| **독립성** | **더빈-왓슨(DW)** — **DW≈2면 자기상관 없음**, 0에 가까우면 양의 자기상관, 4에 가까우면 음의 자기상관 | 자기상관 |
| **등분산성** | 잔차가 **부채꼴로 퍼지면 위배**(이분산성) | 이분산성 |
| **정규성** | **오차항(잔차)**이 정규분포를 따라야 함 (원 변수가 아님!) | Q-Q 플롯 등 |

| 통계량 | 검정 대상 |
|---|---|
| **F검정** | **모형 전체**의 유의성 (모든 회귀계수 = 0인가) |
| **t검정** | **개별** 회귀계수 하나하나의 유의성 |
| **VIF** | **다중공선성** 진단 — **VIF ≥ 10**(엄격히는 5)이면 의심 |
| **AIC/BIC** | 모형 비교 — **값이 작을수록 좋은 모형**. BIC는 log(n) 벌점으로 **복잡한 모형에 더 강한 페널티** |

**암기팁** — **"전체는 F, 개별은 t"** — 회귀분석에서 F와 t가 나오면 "몇 개를 동시에 보는가"로 구분하세요. **정규성은 잔차 얘기, 원본 데이터 얘기가 아닙니다** — "종속변수가 정규분포를 따라야 한다"는 보기는 항상 함정입니다.

**실수 포인트 ⚠️** — 25번에서 정규성 가정의 대상을 헷갈리신 것과, 32·33번에서 F검정·t검정을 반대로 쓰신 것은 **"전체 vs 개별"이라는 축 하나만 세우면 동시에 해결**됩니다. AIC(37번)와 BIC(38번)는 **둘 다 "작을수록 좋다"**는 방향은 같고, **BIC가 표본 크기(n)를 고려해 더 엄격**하다는 차이만 있습니다.

---

#### 1-3. 로지스틱 회귀 · 분류 평가지표

### ⚠️ 43번 — 오즈에 자연로그를 취한 값을 무엇이라 하는가? (헷갈림)

**Q.** 오즈에 자연로그를 취한 값을 무엇이라 하는가? → **정답 ③ 로짓(Logit)**

**해설** — Logit = ln(Odds).

---

### ⚠️ 45번 — 로지스틱 회귀의 회귀계수 β가 0.7일 때 exp(0.7)≈2의 해석으로 옳은 것은? (헷갈림)

**Q.** 로지스틱 회귀의 회귀계수 β가 0.7일 때 exp(0.7)≈2의 해석으로 옳은 것은? → **정답 ① 해당 변수가 1 증가하면 오즈가 약 2배가 된다**

**해설** — exp(β)는 오즈비(Odds Ratio)로, 변수 1 증가 시 오즈의 배율.

---

### ⚠️ 46번 — 로지스틱 회귀의 회귀계수를 추정하는 방법은? (헷갈림)

**Q.** 로지스틱 회귀의 회귀계수를 추정하는 방법은? → **정답 ② 최대우도추정법**

**해설** — 로지스틱 회귀는 최대우도추정법으로 계수를 추정한다.

---

### ⚠️ 47번 — 종속변수의 범주가 3개 이상이고 순서가 없을 때 사용하는 모형은? (헷갈림)

**Q.** 종속변수의 범주가 3개 이상이고 순서가 없을 때 사용하는 모형은? → **정답 ③ 다항(명목) 로지스틱 회귀**

**해설** — 순서 없는 3개 이상 범주 → 다항(명목형) 로지스틱 회귀.

---

### ⚠️ 48번 — 분류 모형의 성능을 평가하기 위해 실제값과 예측값을 교차 집계한 표는? (헷갈림)

**Q.** 분류 모형의 성능을 평가하기 위해 실제값과 예측값을 교차 집계한 표는? → **정답 ④ 혼동행렬(Confusion Matrix)**

**해설** — 실제 vs 예측 교차표 = 혼동행렬.

---

### ❌ 49번 — 전체 예측 중 올바르게 예측한 비율을 나타내는 지표는?

**Q.** 전체 예측 중 올바르게 예측한 비율을 나타내는 지표는?
① 정확도(Accuracy) ② 정밀도(Precision) ③ 재현율(Recall) ④ 특이도(Specificity)

| 내 답 | 정답 |
|---|---|
| ② 정밀도(Precision) | **① 정확도(Accuracy)** |

**해설** — 정확도 = (TP+TN)/전체.

---

### ❌ 50번 — 모델이 Positive로 예측한 것 중 실제 Positive의 비율을 나타내는 지표는?

**Q.** 모델이 Positive로 예측한 것 중 실제 Positive의 비율을 나타내는 지표는?
① 재현율 ② 정밀도 ③ 특이도 ④ 정확도

| 내 답 | 정답 |
|---|---|
| ① 재현율 | **② 정밀도** |

**해설** — 정밀도 = TP / (TP+FP).

---

### ⚠️ 56번 — ROC 곡선의 x축과 y축이 나타내는 것은? (헷갈림)

**Q.** ROC 곡선의 x축과 y축이 나타내는 것은? → **정답 ④ x: 거짓 긍정률(1−특이도), y: 참 긍정률(민감도)**

**해설** — ROC: x축 = FPR(1−특이도), y축 = TPR(민감도).

---

### ⚠️ 57번 — AUC 값에 대한 설명으로 옳은 것은? (헷갈림)

**Q.** AUC 값에 대한 설명으로 옳은 것은? → **정답 ① 1에 가까울수록 분류 성능이 우수하다**

**해설** — AUC는 1에 가까울수록 우수. 0.5는 무작위 수준.

---

### ⚠️ 58번 — AUC가 0.5인 모형에 대한 해석으로 옳은 것은? (헷갈림)

**Q.** AUC가 0.5인 모형에 대한 해석으로 옳은 것은? → **정답 ② 무작위 추측과 다를 바 없다**

**해설** — AUC 0.5 = 동전 던지기와 동일한 성능.

---

### ❌ 59번 — 분류 임계값(Threshold)을 낮출 때 일반적으로 나타나는 변화는?

**Q.** 분류 임계값(Threshold)을 낮출 때 일반적으로 나타나는 변화는?
① 재현율과 정밀도가 모두 감소한다 ② 재현율과 정밀도가 모두 증가한다 ③ 재현율은 증가하고 정밀도는 감소하는 경향이 있다 ④ 아무 변화가 없다

| 내 답 | 정답 |
|---|---|
| ① 재현율과 정밀도가 모두 감소한다 | **③ 재현율은 증가하고 정밀도는 감소하는 경향이 있다** |

**해설** — 임계값↓ → Positive 예측 증가 → 재현율↑, 정밀도↓.

---

> ⚠️ **23문항 중 12문항(52%)이 오답·헷갈림입니다.** 이 시험 전체에서 가장 큰 취약 구간 중 하나입니다. 혼동행렬 기반 지표(정확도·정밀도·재현율)와 로지스틱 회귀 개념(Logit·오즈비·MLE), ROC·AUC가 통째로 흔들립니다.

**개념 정리 — 혼동행렬과 분류 평가지표 총정리 (49·50·59번 동시 해결)**

| 실제\예측 | Positive | Negative |
|---|---|---|
| **Positive** | TP | FN |
| **Negative** | FP | TN |

| 지표 | 공식 | 의미 |
|---|---|---|
| **정확도(Accuracy)** | **(TP+TN) / 전체** | 전체 중 맞힌 비율 |
| **정밀도(Precision)** | **TP / (TP+FP)** | Positive로 예측한 것 중 실제로 맞은 비율 |
| **재현율(Recall)** | TP / (TP+FN) | 실제 Positive 중 맞힌 비율 |

**암기팁** — **"정밀도는 분모가 예측(TP+FP), 재현율은 분모가 실제(TP+FN)"** — 분모에 무엇이 들어가는지만 구분하면 됩니다.

**실수 포인트(59번)** — **임계값을 낮추면 Positive로 예측하는 경우가 늘어나** → 실제 Positive를 더 많이 잡아내므로 **재현율↑**, 대신 틀린 Positive(FP)도 늘어 **정밀도↓**. 재현율·정밀도는 **트레이드오프** 관계입니다.

**개념 정리 — 로지스틱 회귀 기초 (43·45·46·47번 동시 해결)**

| 개념 | 뜻 |
|---|---|
| **Logit** | **ln(Odds)** — 오즈에 로그를 취한 값 |
| **오즈비(exp(β))** | 변수가 1 증가할 때 **오즈가 몇 배**가 되는지 |
| **추정 방법** | **최대우도추정법(MLE)** — 선형회귀의 최소제곱법과 다름 |
| **3개 이상 순서 없는 범주** | **다항(명목형) 로지스틱 회귀** |

**개념 정리 — ROC·AUC (56·57·58번 동시 해결)**

> **ROC 곡선**: x축 = **FPR(1−특이도)**, y축 = **TPR(민감도=재현율)**
> **AUC**: 1에 가까울수록 우수, **0.5 = 무작위 추측(동전 던지기)과 동일한 성능**

**암기팁** — **"로지스틱 회귀는 선형회귀와 이름은 비슷해도 추정법(MLE)이 다르다"**는 것과, **"AUC 0.5는 잘하는 것도 못하는 것도 아닌 정확히 랜덤"**이라는 두 포인트만 확실히 잡아도 이 구간 절반이 풀립니다.

---

#### 1-4. 의사결정나무

### ❌ 67번 — 분류나무에서 분리 기준으로 사용되지 않는 것은?

**Q.** 분류나무에서 분리 기준으로 사용되지 않는 것은?
① 지니 지수 ② 엔트로피 지수 ③ 결정계수 ④ 카이제곱 통계량

| 내 답 | 정답 |
|---|---|
| ② 엔트로피 지수 | **③ 결정계수** |

**해설** — 결정계수는 회귀 평가 지표. 분류나무 기준 = 지니·엔트로피·카이제곱.

---

### ❌ 68번 — 회귀나무에서 분리 기준으로 사용하는 것은?

**Q.** 회귀나무에서 분리 기준으로 사용하는 것은?
① 지니 지수 ② 정보 이득비 ③ 엔트로피 ④ 분산 감소량(F 통계량)

| 내 답 | 정답 |
|---|---|
| ① 지니 지수 | **④ 분산 감소량(F 통계량)** |

**해설** — 회귀나무는 분산 감소량(F 통계량)을 기준으로 분리.

---

### ⚠️ 69번 — 지니 지수(Gini Index)에 대한 설명으로 옳은 것은? (헷갈림)

**Q.** 지니 지수(Gini Index)에 대한 설명으로 옳은 것은? → **정답 ① 값이 0이면 완전히 순수한 노드이다**

**해설** — 지니 지수 0 = 완전 순수. 클수록 불순도가 높다.

---

### ❌ 70번 — 부모 노드의 엔트로피에서 자식 노드들의 가중 평균 엔트로피를 뺀 값을 무엇이라 하는가?

**Q.** 부모 노드의 엔트로피에서 자식 노드들의 가중 평균 엔트로피를 뺀 값을 무엇이라 하는가?
① 지니 지수 ② 정보 이득(Information Gain) ③ 불순도 ④ 카이제곱 값

| 내 답 | 정답 |
|---|---|
| ③ 불순도 | **② 정보 이득(Information Gain)** |

**해설** — 정보 이득 = 부모 엔트로피 − 자식들의 가중 엔트로피.

---

### ⚠️ 71번 — 지니 지수를 분리 기준으로 사용하며 이진 분리만 수행하는 의사결정나무 알고리즘은? (헷갈림)

**Q.** 지니 지수를 분리 기준으로 사용하며 이진 분리만 수행하는 의사결정나무 알고리즘은? → **정답 ③ CART**

**해설** — CART = 지니 지수 + 이진 분리.

---

### ⚠️ 72번 — 카이제곱 통계량을 이용하여 다지 분리를 수행하는 의사결정나무 알고리즘은? (헷갈림)

**Q.** 카이제곱 통계량을 이용하여 다지 분리를 수행하는 의사결정나무 알고리즘은? → **정답 ④ CHAID**

**해설** — CHAID = 카이제곱 기반 다지 분리.

---

### ❌ 73번 — 엔트로피 기반 정보 이득비를 사용하는 알고리즘은?

**Q.** 엔트로피 기반 정보 이득비를 사용하는 알고리즘은?
① C4.5 ② CHAID ③ CART ④ K-means

| 내 답 | 정답 |
|---|---|
| ③ CART | **① C4.5** |

**해설** — C4.5 = 엔트로피 기반 정보 이득비.

---

> ⚠️ **13문항 중 7문항(54%)이 오답·헷갈림입니다.** 의사결정나무 알고리즘(CART·C4.5·CHAID)과 분리 기준(지니·엔트로피·정보이득·분산감소)이 섞여 있습니다.

**개념 정리 — 의사결정나무 알고리즘 3형제 (69·71·72·73번 동시 해결)**

| 알고리즘 | 분리 기준 | 분리 방식 |
|---|---|---|
| **CART** | **지니 지수**(분류) / 분산 감소(회귀) | **이진 분리**만 |
| **C4.5** | **엔트로피 기반 정보 이득비** | 다지 분리 가능 |
| **CHAID** | **카이제곱 통계량** | 다지 분리 |

**개념 정리 — 분리 기준 공식 (67·68·70번 동시 해결)**

| 기준 | 사용 대상 | 공식·의미 |
|---|---|---|
| **지니 지수** | 분류나무 | **0 = 완전 순수**, 클수록 불순도↑ |
| **엔트로피·정보 이득** | 분류나무 | **정보 이득 = 부모 엔트로피 − 자식들의 가중 엔트로피** |
| **분산 감소량(F통계량)** | **회귀나무** | 분리 후 분산이 얼마나 줄었는가 |
| 결정계수(R²) | (나무 기준 아님) | **회귀 "평가"** 지표이지 나무의 분리 기준이 아님 |

**암기팁** — **"CART=이진만(Classification And Regression Tree, 딱 둘로만), C4.5=다지 가능(엔트로피), CHAID=카이제곱(범주형에 강함)"**

**실수 포인트 ⚠️** — 67번에서 "결정계수"를 분류나무 기준으로 착각하기 쉬운데, **결정계수(R²)는 회귀 모델의 "평가" 지표**이지 나무를 "분리"하는 기준이 아닙니다. 68번(회귀나무 분리 기준)과 헷갈리지 않도록, **"분리 기준"과 "평가 지표"를 서로 다른 질문으로 구분**하세요.

---

#### 1-5. 인공신경망

### ⚠️ 78번 — 단층 퍼셉트론이 해결할 수 없는 대표적인 문제는? (헷갈림)

**Q.** 단층 퍼셉트론이 해결할 수 없는 대표적인 문제는? → **정답 ② XOR 문제**

**해설** — 단층 퍼셉트론은 선형 분리만 가능해 XOR을 풀 수 없다.

---

### ⚠️ 80번 — 인공신경망에서 은닉층의 노드 수가 지나치게 많을 때 발생하기 쉬운 문제는? (헷갈림)

**Q.** 인공신경망에서 은닉층의 노드 수가 지나치게 많을 때 발생하기 쉬운 문제는? → **정답 ④ 과적합**

**해설** — 은닉 노드가 과다하면 모델이 복잡해져 과적합.

---

### ❌ 82번 — 출력 범위가 −1에서 1이며 시그모이드보다 중심이 0에 가까운 활성화 함수는?

**Q.** 출력 범위가 −1에서 1이며 시그모이드보다 중심이 0에 가까운 활성화 함수는?
① ReLU ② tanh ③ 계단함수 ④ 소프트맥스

| 내 답 | 정답 |
|---|---|
| ④ 소프트맥스 | **② tanh** |

**해설** — tanh는 −1~1 출력, 0 중심이라 시그모이드보다 학습에 유리.

---

### ❌ 84번 — 다중 분류 문제의 출력층에서 각 클래스의 확률 합이 1이 되도록 만드는 함수는?

**Q.** 다중 분류 문제의 출력층에서 각 클래스의 확률 합이 1이 되도록 만드는 함수는?
① Leaky ReLU ② 시그모이드 ③ ReLU ④ 소프트맥스

| 내 답 | 정답 |
|---|---|
| ① Leaky ReLU | **④ 소프트맥스** |

**해설** — 소프트맥스는 출력 합이 1인 확률 분포를 만든다.

---

### ⚠️ 89번 — 전체 데이터를 여러 개의 묶음으로 나누어 순차적으로 학습시킬 때 그 묶음을 무엇이라 하는가? (헷갈림)

**Q.** 전체 데이터를 여러 개의 묶음으로 나누어 순차적으로 학습시킬 때 그 묶음을 무엇이라 하는가? → **정답 ④ 배치(미니배치)**

**해설** — 나눠진 데이터 묶음 = 배치(미니배치).

---

### ⚠️ 90번 — 전체 학습 데이터를 한 번 모두 학습한 횟수를 나타내는 용어는? (헷갈림)

**Q.** 전체 학습 데이터를 한 번 모두 학습한 횟수를 나타내는 용어는? → **정답 ③ 에포크**

**해설** — 전체 데이터 1회 학습 = 1 에포크.

---

### ⚠️ 92번 — 각 층의 입력 분포를 정규화하여 학습을 안정화하고 속도를 높이는 기법은? (헷갈림)

**Q.** 각 층의 입력 분포를 정규화하여 학습을 안정화하고 속도를 높이는 기법은? → **정답 ④ 배치 정규화**

**해설** — 배치 정규화 = 층 입력 분포 정규화로 학습 안정화·가속.

---

### ❌ 93번 — 검증 오차가 더 이상 개선되지 않을 때 학습을 중단하는 과적합 방지 기법은?

**Q.** 검증 오차가 더 이상 개선되지 않을 때 학습을 중단하는 과적합 방지 기법은?
① 배치 정규화 ② 조기 종료(Early Stopping) ③ 부스팅 ④ 정지 규칙

| 내 답 | 정답 |
|---|---|
| ④ 정지 규칙 | **② 조기 종료(Early Stopping)** |

**해설** — 조기 종료 = 검증 오차가 나빠지기 시작할 때 학습 중단.

---

### ⚠️ 95번 — 모멘텀과 적응적 학습률을 결합하여 널리 사용되는 최적화 알고리즘은? (헷갈림)

**Q.** 모멘텀과 적응적 학습률을 결합하여 널리 사용되는 최적화 알고리즘은? → **정답 ③ Adam**

**해설** — Adam = 모멘텀 + RMSProp(적응적 학습률).

---

> ⚠️ **19문항 중 9문항(47%)이 오답·헷갈림입니다.** 활성화 함수, 학습 기본 용어(배치·에포크·조기종료), 최적화 기법(Adam)이 섞여 있습니다.

**개념 정리 — 활성화 함수 비교 (82·84번 동시 해결)**

| 함수 | 출력 범위 | 특징 |
|---|---|---|
| 시그모이드 | 0~1 | 0 중심이 아님(학습 비효율) |
| **tanh** | **−1~1** | **0 중심**이라 시그모이드보다 학습에 유리 |
| **소프트맥스** | 각 클래스 확률 | **출력 합이 1**인 확률 분포(다중분류 출력층) |
| ReLU | 0~∞ | 음수는 0, 기울기 소실 완화 |

**개념 정리 — 신경망 학습 기본 용어 (89·90·93번 동시 해결)**

| 용어 | 뜻 |
|---|---|
| **배치(미니배치)** | 한 번에 학습에 사용하는 **데이터 묶음** |
| **에포크(Epoch)** | **전체 데이터를 1회** 학습 완료 |
| **조기 종료(Early Stopping)** | **검증 오차가 나빠지기 시작**할 때 학습을 멈춤(과적합 방지) |
| **배치 정규화** | 층 입력의 분포를 정규화해 **학습을 안정화·가속** |
| **Adam** | **모멘텀 + RMSProp**(적응적 학습률) 결합 옵티마이저 |

**암기팁** — **"조기 종료는 '검증' 오차 기준"**입니다(학습 오차가 아님). 학습 오차는 계속 줄어들 수 있어도, 검증 오차가 오히려 늘기 시작하는 순간이 과적합의 신호입니다.

**실수 포인트 ⚠️** — 78번(단층 퍼셉트론은 선형 분리만 가능해 XOR 불가)과 80번(은닉 노드 과다→과적합)은 **신경망의 구조(단순함↔복잡함)가 성능에 미치는 영향**을 묻는 문제로, **"너무 단순하면 못 풀고(과소적합=XOR불가), 너무 복잡하면 과적합"**이라는 1과목·2과목에서도 반복된 편향-분산 원칙이 그대로 적용됩니다.

---

#### 1-6. SVM · KNN · 나이브베이즈

### ❌ 99번 — 선형 분리가 불가능한 데이터를 고차원 공간으로 사상하여 분리하는 SVM의 기법은?

**Q.** 선형 분리가 불가능한 데이터를 고차원 공간으로 사상하여 분리하는 SVM의 기법은?
① 배깅 ② 가지치기 ③ 커널 트릭 ④ 드롭아웃

| 내 답 | 정답 |
|---|---|
| ① 배깅 | **③ 커널 트릭** |

**해설** — 커널 트릭으로 고차원 사상 없이 내적만 계산해 비선형 분리.

---

### ❌ 102번 — KNN을 게으른 학습(Lazy Learning)이라 부르는 이유는?

**Q.** KNN을 게으른 학습(Lazy Learning)이라 부르는 이유는?
① 학습 속도가 느리기 때문 ② 학습 단계에서 모델을 구축하지 않고 예측 시점에 계산하기 때문 ③ 정확도가 낮기 때문 ④ 결측값을 처리하지 못하기 때문

| 내 답 | 정답 |
|---|---|
| ① 학습 속도가 느리기 때문 | **② 학습 단계에서 모델을 구축하지 않고 예측 시점에 계산하기 때문** |

**해설** — 사전에 모델을 만들지 않고 예측 시점에 계산 → 게으른 학습.

---

### ⚠️ 103번 — KNN에서 K값을 지나치게 작게 설정할 때 발생하는 문제는? (헷갈림)

**Q.** KNN에서 K값을 지나치게 작게 설정할 때 발생하는 문제는? → **정답 ③ 노이즈에 민감해져 과적합이 발생한다**

**해설** — K가 작으면 노이즈에 민감(과적합), 크면 경계가 뭉개짐(과소적합).

---

> ⚠️ **10문항 중 3문항(30%)** — 이번 배치에서 상대적으로 안정적인 구간입니다. SVM의 커널 트릭, KNN의 게으른 학습, K값에 따른 과적합/과소적합만 짚어두면 됩니다.

**개념 정리**

| 개념 | 핵심 |
|---|---|
| **커널 트릭(SVM)** | 고차원으로 실제 사상하지 않고 **내적만 계산**해 비선형 분리를 수행 |
| **게으른 학습(KNN)** | **사전에 모델을 만들지 않고**, 예측 시점에 그때그때 계산 |
| **K값(KNN)** | **K 작음 → 노이즈에 민감(과적합)**, **K 큼 → 경계가 뭉개짐(과소적합)** |

**암기팁** — **"KNN은 게으르다(Lazy) — 미리 공부 안 하고 시험장에서 벼락치기"**로 기억하면 "사전 모델 없음"이라는 특징이 자연스럽게 연결됩니다.

**실수 포인트 ⚠️** — 99번(커널 트릭)에서 "고차원으로 사상한다"는 것과 "실제로 사상하지 않고 내적만 계산한다"는 것을 헷갈리기 쉬운데, **커널 트릭의 핵심은 바로 이 "실제 계산을 생략"하는 효율성**입니다.

---

#### 1-7. 연관분석

### ❌ 107번 — 전체 거래 중 항목 A와 B를 동시에 포함하는 거래의 비율을 나타내는 지표는?

**Q.** 전체 거래 중 항목 A와 B를 동시에 포함하는 거래의 비율을 나타내는 지표는?
① 향상도(Lift) ② 신뢰도(Confidence) ③ 지지도(Support) ④ 정확도

| 내 답 | 정답 |
|---|---|
| ② 신뢰도(Confidence) | **③ 지지도(Support)** |

**해설** — 지지도 = P(A∩B).

---

### ❌ 108번 — A를 구매한 거래 중 B도 함께 구매한 비율을 나타내는 지표는?

**Q.** A를 구매한 거래 중 B도 함께 구매한 비율을 나타내는 지표는?
① 지지도 ② 재현율 ③ 향상도 ④ 신뢰도

| 내 답 | 정답 |
|---|---|
| ② 재현율 | **④ 신뢰도** |

**해설** — 신뢰도 = P(B\|A) = 지지도(A∩B)/지지도(A).

---

### ❌ 111번 — "빈발 항목집합의 모든 부분집합도 빈발하다"는 성질을 이용해 탐색 공간을 줄이는 알고리즘은?

**Q.** "빈발 항목집합의 모든 부분집합도 빈발하다"는 성질을 이용해 탐색 공간을 줄이는 알고리즘은?
① FP-Growth ② Apriori ③ K-means ④ DBSCAN

| 내 답 | 정답 |
|---|---|
| ① FP-Growth | **② Apriori** |

**해설** — Apriori의 핵심 = 빈발 항목집합의 부분집합도 빈발(하향 폐쇄성).

---

### ❌ 112번 — Apriori 알고리즘의 단점으로 옳은 것은?

**Q.** Apriori 알고리즘의 단점으로 옳은 것은?
① 규칙 해석이 불가능하다 ② 지지도를 계산할 수 없다 ③ 후보 집합 생성과 데이터베이스 반복 스캔으로 계산 비용이 크다 ④ 범주형 데이터에 적용할 수 없다

| 내 답 | 정답 |
|---|---|
| ④ 범주형 데이터에 적용할 수 없다 | **③ 후보 집합 생성과 데이터베이스 반복 스캔으로 계산 비용이 크다** |

**해설** — 후보 생성 + DB 반복 스캔으로 계산 비용이 크다.

---

### ❌ 113번 — 후보 집합을 생성하지 않고 트리 구조로 빈발 패턴을 탐색해 속도를 높인 알고리즘은?

**Q.** 후보 집합을 생성하지 않고 트리 구조로 빈발 패턴을 탐색해 속도를 높인 알고리즘은?
① Apriori ② Adaboost ③ CART ④ FP-Growth

| 내 답 | 정답 |
|---|---|
| ③ CART | **④ FP-Growth** |

**해설** — FP-Growth = FP-Tree로 후보 생성 없이 탐색.

---

> ⚠️ **9문항 중 5문항(56%)이 오답입니다.** 지지도·신뢰도·향상도 공식과 Apriori·FP-Growth의 차이가 흔들립니다. 헷갈림 없이 전부 확실한 오답이라, 공식 자체를 처음부터 다시 잡아야 하는 구간입니다.

**개념 정리 — 연관규칙 3대 지표 (107·108번 동시 해결)**

| 지표 | 공식 | 의미 |
|---|---|---|
| **지지도(Support)** | **P(A∩B)** | 전체 거래 중 A, B **동시** 발생 비율 |
| **신뢰도(Confidence)** | **P(B\|A) = 지지도(A∩B) / 지지도(A)** | A를 산 사람 중 B도 산 **조건부 확률** |
| 향상도(Lift) | 신뢰도 / P(B) | A,B가 **우연보다 얼마나 더** 같이 팔리는가(1 기준) |

**개념 정리 — Apriori vs FP-Growth (111·112·113번 동시 해결)**

| 알고리즘 | 방식 | 특징 |
|---|---|---|
| **Apriori** | **후보 항목집합을 생성**하고 DB를 **반복 스캔** | **"빈발 항목집합의 부분집합도 빈발"**(하향 폐쇄성) 원리로 탐색 공간을 줄임. 그래도 **계산 비용이 큼** |
| **FP-Growth** | **FP-Tree**(트리 구조)로 데이터를 압축 | **후보 생성 없이** 트리를 탐색해 **더 빠름** |

**암기팁** — **"신뢰도는 조건부 확률(A를 산 사람 "안에서" B)"**, **"지지도는 전체 중에서(A,B 둘 다)"** — 분모가 "A만"인지 "전체"인지로 구분하세요. **"FP = Frequent Pattern, 후보 없이 트리(Tree)로 빠르게"**가 Apriori와의 결정적 차이입니다.

**실수 포인트 ⚠️** — 5문항 전부 오답이라는 것은 **공식 자체가 아직 낯설다는 뜻**입니다. 찍어서 맞힌 게 하나도 없다는 건 오히려 "정직한 신호"이니, 위 두 표만 새로 확실히 암기하면 이 영역은 빠르게 회복됩니다.

---

#### 1-8. 군집분석

### ⚠️ 120번 — 이상값의 영향을 줄이기 위해 군집의 중심으로 평균 대신 실제 관측치를 사용하는 기법은? (헷갈림)

**Q.** 이상값의 영향을 줄이기 위해 군집의 중심으로 평균 대신 실제 관측치를 사용하는 기법은? → **정답 ④ K-medoids(PAM)**

**해설** — K-medoids는 중심으로 실제 관측치를 사용해 이상값에 강건.

---

### ⚠️ 121번 — 개체를 하나씩 병합하거나 분할하며 나무 구조를 만드는 군집 기법은? (헷갈림)

**Q.** 개체를 하나씩 병합하거나 분할하며 나무 구조를 만드는 군집 기법은? → **정답 ① 계층적 군집**

**해설** — 병합·분할로 계층 구조를 만드는 것 = 계층적 군집.

---

### ⚠️ 122번 — 계층적 군집의 결과를 나무 형태로 시각화한 그래프는? (헷갈림)

**Q.** 계층적 군집의 결과를 나무 형태로 시각화한 그래프는? → **정답 ② 덴드로그램**

**해설** — 계층적 군집의 시각화 = 덴드로그램.

---

### ❌ 123번 — 두 군집 간 거리를 각 군집에서 가장 가까운 두 개체 사이의 거리로 정의하는 연결법은?

**Q.** 두 군집 간 거리를 각 군집에서 가장 가까운 두 개체 사이의 거리로 정의하는 연결법은?
① 평균 연결법 ② 최장 연결법 ③ 최단 연결법 ④ 와드 연결법

| 내 답 | 정답 |
|---|---|
| ② 최장 연결법 | **③ 최단 연결법** |

**해설** — 최단 연결법(단일 연결법) = 가장 가까운 두 점의 거리.

---

### ⚠️ 124번 — 두 군집을 병합했을 때 증가하는 오차제곱합(군집 내 분산)을 최소화하는 방향으로 병합하는 연결법은? (헷갈림)

**Q.** 두 군집을 병합했을 때 증가하는 오차제곱합(군집 내 분산)을 최소화하는 방향으로 병합하는 연결법은? → **정답 ④ 와드 연결법**

**해설** — 와드 연결법 = 병합 시 오차제곱합 증가를 최소화.

---

### ❌ 125번 — 최단 연결법의 특징으로 옳은 것은?

**Q.** 최단 연결법의 특징으로 옳은 것은?
① 고립된 이상값 탐지에 유용하지만 군집이 길게 늘어지는 사슬 효과가 나타난다 ② 항상 구형 군집을 만든다 ③ 계산이 불가능하다 ④ 군집 수를 미리 정해야 한다

| 내 답 | 정답 |
|---|---|
| ② 항상 구형 군집을 만든다 | **① 고립된 이상값 탐지에 유용하지만 군집이 길게 늘어지는 사슬 효과가 나타난다** |

**해설** — 최단 연결법은 사슬 효과(chaining)로 군집이 길게 늘어질 수 있다.

---

### ⚠️ 128번 — 변수 간 상관관계와 분산을 고려하여 거리를 표준화한 척도는? (헷갈림)

**Q.** 변수 간 상관관계와 분산을 고려하여 거리를 표준화한 척도는? → **정답 ④ 마할라노비스 거리**

**해설** — 마할라노비스 거리 = 공분산을 반영한 표준화 거리.

---

### ⚠️ 129번 — 두 벡터가 이루는 각도를 이용하여 유사도를 측정하며 문서 유사도 계산에 널리 쓰이는 척도는? (헷갈림)

**Q.** 두 벡터가 이루는 각도를 이용하여 유사도를 측정하며 문서 유사도 계산에 널리 쓰이는 척도는? → **정답 ① 코사인 유사도**

**해설** — 코사인 유사도 = 벡터 간 각도, 문서 유사도에 사용.

---

### ⚠️ 130번 — 두 집합의 교집합 크기를 합집합 크기로 나누어 유사도를 계산하는 척도는? (헷갈림)

**Q.** 두 집합의 교집합 크기를 합집합 크기로 나누어 유사도를 계산하는 척도는? → **정답 ② 자카드 계수**

**해설** — 자카드 계수 = 교집합/합집합.

---

### ⚠️ 133번 — 데이터가 여러 개의 확률분포(주로 정규분포)의 혼합으로 생성되었다고 가정하는 군집 기법은? (헷갈림)

**Q.** 데이터가 여러 개의 확률분포(주로 정규분포)의 혼합으로 생성되었다고 가정하는 군집 기법은? → **정답 ① 혼합분포 군집(GMM)**

**해설** — 혼합분포 군집(GMM) = 여러 정규분포의 혼합으로 가정.

---

### ⚠️ 134번 — 혼합분포 군집에서 모수를 추정할 때 사용하는 반복 알고리즘은? (헷갈림)

**Q.** 혼합분포 군집에서 모수를 추정할 때 사용하는 반복 알고리즘은? → **정답 ② EM 알고리즘**

**해설** — GMM의 모수 추정은 EM(기댓값-최대화) 알고리즘.

---

### ⚠️ 135번 — 고차원 데이터를 2차원 격자에 사상하여 시각화하는 비지도 신경망 군집 기법은? (헷갈림)

**Q.** 고차원 데이터를 2차원 격자에 사상하여 시각화하는 비지도 신경망 군집 기법은? → **정답 ③ 자기조직화지도(SOM)**

**해설** — SOM = 코호넨 맵, 고차원을 2차원 격자로 사상.

---

> ⚠️ **21문항 중 12문항(57%)이 오답·헷갈림입니다.** 특히 **헷갈림이 10개**로 이 시험 전체에서 가장 많이 몰린 구간입니다 — 오답은 적지만 확신 없이 찍어 맞힌 게 대부분이라, 실전에서는 위험한 영역입니다. 계층적 군집의 연결법, 비계층적 군집(K-medoids·GMM·SOM), 거리·유사도 측도가 뒤섞여 있습니다.

**개념 정리 — 군집분석 기법 총정리 (120~135번 구간 전체 해결)**

| 구분 | 기법 | 핵심 |
|---|---|---|
| **계층적** | 병합·분할로 계층 구조 형성 | 시각화 = **덴드로그램** |
| ↳ 연결법 | **최단(단일) 연결법** | 가장 가까운 두 점의 거리 → **사슬 효과**로 군집이 길게 늘어짐 |
| ↳ 연결법 | **와드 연결법** | 병합 시 **오차제곱합 증가를 최소화**하는 방향으로 병합 |
| **비계층적** | **K-medoids** | 중심으로 **실제 관측치**를 사용 → **이상값에 강건**(K-means는 평균점이라 약함) |
| **비계층적** | **GMM(혼합분포 군집)** | 여러 **정규분포의 혼합**으로 가정, **EM 알고리즘**으로 모수 추정 |
| **비계층적** | **SOM(자기조직화지도)** | **코호넨 맵** — 고차원을 **2차원 격자**로 사상 |

**개념 정리 — 거리·유사도 측도**

| 측도 | 반영하는 것 | 주 용도 |
|---|---|---|
| **마할라노비스 거리** | **공분산을 반영**한 표준화 거리 | 변수 간 상관 고려 |
| **코사인 유사도** | 벡터 간 **각도** | **문서 유사도**(방향이 중요, 크기 무관) |
| **자카드 계수** | **교집합/합집합** | 집합 간 유사도(예: 태그, 구매목록) |

**암기팁** — **"K-medoids는 진짜 존재하는 점(medoid)을 중심으로, K-means는 가짜 평균점을 중심으로"** — 그래서 K-medoids가 이상값에 강합니다. **"GMM=정규분포 여러 개를 섞음(Gaussian Mixture)", "SOM=지도(Map)처럼 2차원에 펼침"**으로 이름 그대로 연상하세요.

**실수 포인트 ⚠️** — 헷갈림이 10개나 된다는 것은 **"찍었는데 맞았다"**는 뜻이라, 다음 시험에서는 틀릴 가능성이 높습니다. 위 표를 **눈으로 읽지만 말고 직접 손으로 한 번 그려보는 것**을 권합니다 — 이 영역이 이번 배치 중 확신도가 가장 낮은 곳입니다.

---

#### 1-9. 앙상블

### ⚠️ 136번 — 여러 개의 약한 학습기를 결합하여 예측 성능을 높이는 기법을 총칭하는 말은? (헷갈림)

**Q.** 여러 개의 약한 학습기를 결합하여 예측 성능을 높이는 기법을 총칭하는 말은? → **정답 ④ 앙상블**

**해설** — 여러 약한 학습기 결합 = 앙상블.

---

### ⚠️ 139번 — 배깅과 부스팅의 비교로 옳은 것은? (헷갈림)

**Q.** 배깅과 부스팅의 비교로 옳은 것은? → **정답 ③ 배깅은 병렬 학습으로 분산을 줄이고, 부스팅은 순차 학습으로 편향을 줄인다**

**해설** — 배깅은 분산 감소(병렬), 부스팅은 편향 감소(순차).

---

### ⚠️ 141번 — 여러 모델의 예측값을 새로운 입력으로 삼아 최종 메타 모델을 학습시키는 앙상블 기법은? (헷갈림)

**Q.** 여러 모델의 예측값을 새로운 입력으로 삼아 최종 메타 모델을 학습시키는 앙상블 기법은? → **정답 ① 스태킹(Stacking)**

**해설** — 스태킹 = 개별 모델 예측값을 입력으로 메타 모델 학습.

---

### ❌ 142번 — 배깅에 변수(피처)를 무작위로 선택하는 과정을 추가한 앙상블 모델은?

**Q.** 배깅에 변수(피처)를 무작위로 선택하는 과정을 추가한 앙상블 모델은?
① AdaBoost ② 랜덤포레스트 ③ XGBoost ④ SVM

| 내 답 | 정답 |
|---|---|
| ① AdaBoost | **② 랜덤포레스트** |

**해설** — 랜덤포레스트 = 배깅 + 무작위 변수 선택.

---

### ⚠️ 144번 — 오분류된 관측치의 가중치를 높이며 약한 분류기를 순차적으로 결합하는 초기 부스팅 알고리즘은? (헷갈림)

**Q.** 오분류된 관측치의 가중치를 높이며 약한 분류기를 순차적으로 결합하는 초기 부스팅 알고리즘은? → **정답 ④ AdaBoost**

**해설** — AdaBoost = 오분류 샘플 가중치를 높이는 적응형 부스팅.

---

### ❌ 145번 — 이전 모델의 잔차(오차)를 새로운 모델이 학습하도록 하는 부스팅 방식은?

**Q.** 이전 모델의 잔차(오차)를 새로운 모델이 학습하도록 하는 부스팅 방식은?
① 그래디언트 부스팅(GBM) ② 배깅 ③ 보팅 ④ SOM

| 내 답 | 정답 |
|---|---|
| ④ SOM | **① 그래디언트 부스팅(GBM)** |

**해설** — GBM = 이전 모델의 잔차를 다음 모델이 학습.

---

### ⚠️ 146번 — GBM에 규제와 병렬 처리를 도입해 속도와 성능을 개선한 알고리즘은? (헷갈림)

**Q.** GBM에 규제와 병렬 처리를 도입해 속도와 성능을 개선한 알고리즘은? → **정답 ② XGBoost**

**해설** — XGBoost = GBM + 규제 + 병렬 처리로 개선.

---

### ⚠️ 147번 — 랜덤포레스트에서 부트스트랩 표본에 뽑히지 않은 데이터로 성능을 평가하는 방식은? (헷갈림)

**Q.** 랜덤포레스트에서 부트스트랩 표본에 뽑히지 않은 데이터로 성능을 평가하는 방식은? → **정답 ③ OOB(Out-Of-Bag) 평가**

**해설** — OOB = 부트스트랩에서 뽑히지 않은 약 36.8%로 성능 평가.

---

> ⚠️ **12문항 중 8문항(67%)이 오답·헷갈림입니다.** 오답은 적지만(2개) 헷갈림이 6개로, 앙상블 기법 전체(배깅·부스팅·스태킹)의 세부 개념이 아직 확신 없는 상태입니다.

**개념 정리 — 앙상블 기법 총정리 (136~147번 구간 전체 해결)**

| 구분 | 방식 | 대표 기법 |
|---|---|---|
| **배깅(Bagging)** | **병렬**로 여러 모델 학습 → **분산 감소** | **랜덤포레스트**(배깅 + 무작위 변수 선택) |
| **부스팅(Boosting)** | **순차**로 이전 모델의 오류를 보완 → **편향 감소** | **AdaBoost**(오분류 샘플 가중치↑), **GBM**(이전 모델의 **잔차**를 다음 모델이 학습), **XGBoost**(GBM+규제+병렬처리로 개선) |
| **스태킹(Stacking)** | 개별 모델의 **예측값을 입력**으로 **메타 모델**을 학습 | — |

**개념 정리 — OOB(Out-of-Bag)**

> 부트스트랩 표본을 뽑을 때 **선택되지 않는 약 36.8%**의 데이터로 **별도의 평가용 데이터 없이도 성능을 검증**할 수 있습니다.

**암기팁** — **"배깅=병렬(Bagging=Bootstrap Aggregating, 여러 개를 한꺼번에)", "부스팅=순차(이전 것을 부스트업)"** — 병렬/순차만 구분하면 분산↓/편향↓ 방향이 자동으로 따라옵니다. **"GBM은 잔차를 학습, AdaBoost는 가중치를 조정"**이 둘의 결정적 차이입니다.

**실수 포인트 ⚠️** — 142번(랜덤포레스트=배깅+무작위 변수선택)에서 "배깅"만 있고 "무작위 변수 선택"을 빼먹기 쉬운데, **랜덤포레스트가 일반 배깅과 다른 지점이 바로 이 "변수까지 무작위로 선택"**한다는 점입니다.

---

#### 1-10. 딥러닝

### ⚠️ 152번 — 순차적·시계열 데이터를 처리하기 위해 이전 시점의 출력을 다시 입력으로 사용하는 신경망은? (헷갈림)

**Q.** 순차적·시계열 데이터를 처리하기 위해 이전 시점의 출력을 다시 입력으로 사용하는 신경망은? → **정답 ④ RNN(순환 신경망)**

**해설** — RNN = 이전 시점 은닉 상태를 다시 입력으로 사용.

---

### ❌ 153번 — RNN의 장기 의존성 문제를 해결하기 위해 입력·망각·출력 게이트를 도입한 구조는?

**Q.** RNN의 장기 의존성 문제를 해결하기 위해 입력·망각·출력 게이트를 도입한 구조는?
① LSTM ② CNN ③ 오토인코더 ④ 퍼셉트론

| 내 답 | 정답 |
|---|---|
| ③ 오토인코더 | **① LSTM** |

**해설** — LSTM = 입력·망각·출력 게이트로 장기 의존성 문제 해결.

---

### ❌ 154번 — LSTM을 단순화하여 게이트를 2개로 줄인 구조는?

**Q.** LSTM을 단순화하여 게이트를 2개로 줄인 구조는?
① CNN ② GRU ③ GAN ④ RBM

| 내 답 | 정답 |
|---|---|
| ④ RBM | **② GRU** |

**해설** — GRU = 업데이트·리셋 게이트 2개로 단순화한 LSTM 변형.

---

### ⚠️ 156번 — 입력을 압축했다가 다시 복원하도록 학습시켜 특징 추출·차원 축소에 사용하는 비지도 신경망은? (헷갈림)

**Q.** 입력을 압축했다가 다시 복원하도록 학습시켜 특징 추출·차원 축소에 사용하는 비지도 신경망은? → **정답 ④ 오토인코더**

**해설** — 오토인코더 = 인코더로 압축, 디코더로 복원하는 비지도 신경망.

---

### ❌ 158번 — 분류 문제에서 주로 사용하는 손실함수는?

**Q.** 분류 문제에서 주로 사용하는 손실함수는?
① 평균제곱오차(MSE) ② 교차 엔트로피(Cross Entropy) ③ 지니 지수 ④ 실루엣 계수

| 내 답 | 정답 |
|---|---|
| ③ 지니 지수 | **② 교차 엔트로피(Cross Entropy)** |

**해설** — 분류 손실함수 = 교차 엔트로피.

---

> ⚠️ **12문항 중 5문항(42%)이 오답·헷갈림입니다.** RNN 계열(RNN·LSTM·GRU)의 차이와 오토인코더, 분류 손실함수가 흔들립니다.

**개념 정리 — RNN 계열 비교 (152·153·154번 동시 해결)**

| 구조 | 핵심 |
|---|---|
| **RNN** | **이전 시점의 은닉 상태**를 다시 입력으로 사용 (순환) |
| **LSTM** | **입력·망각·출력 게이트** 3개로 **장기 의존성 문제**(기울기 소실)를 해결 |
| **GRU** | **업데이트·리셋 게이트** 2개로 LSTM을 **단순화**한 변형 |

**암기팁** — **"게이트가 3개면 LSTM, 2개면 GRU"** — GRU는 LSTM의 경량화 버전이라고 기억하면 게이트 개수 차이가 자연스럽게 연결됩니다.

**개념 정리 — 기타**

| 개념 | 핵심 |
|---|---|
| **오토인코더** | **인코더**로 압축 → **디코더**로 복원하는 **비지도** 신경망 |
| **분류 손실함수** | **교차 엔트로피**(회귀는 MSE) |

**실수 포인트 ⚠️** — 오토인코더는 **"정답 레이블 없이" 입력 자체를 복원하도록 학습**하므로 비지도학습입니다. 158번(분류 손실함수=교차 엔트로피)은 회귀의 MSE와 짝지어 **"분류=교차엔트로피, 회귀=MSE"**로 기억하세요.

---

#### 1-11. 시계열 분석 ⭐최위험 구간⭐

### ❌ 160번 — 시계열 데이터에서 평균과 분산이 시간에 따라 일정하고 공분산이 시차에만 의존하는 성질은?

**Q.** 시계열 데이터에서 평균과 분산이 시간에 따라 일정하고 공분산이 시차에만 의존하는 성질은?
① 자기상관 ② 계절성 ③ 추세 ④ 정상성(Stationarity)

| 내 답 | 정답 |
|---|---|
| ③ 추세 | **④ 정상성(Stationarity)** |

**해설** — 정상성 = 평균·분산 일정, 공분산은 시차에만 의존.

---

### ⚠️ 161번 — 비정상 시계열을 정상 시계열로 변환하기 위해 일반적으로 사용하는 방법은? (헷갈림)

**Q.** 비정상 시계열을 정상 시계열로 변환하기 위해 일반적으로 사용하는 방법은? → **정답 ① 차분(Differencing)**

**해설** — 평균이 일정하지 않은 비정상 시계열은 차분으로 안정화.

---

### ⚠️ 162번 — 분산이 시간에 따라 변하는 비정상 시계열을 안정화하기 위해 사용하는 변환은? (헷갈림)

**Q.** 분산이 시간에 따라 변하는 비정상 시계열을 안정화하기 위해 사용하는 변환은? → **정답 ② 로그 변환**

**해설** — 분산이 변하는 경우 로그(또는 Box-Cox) 변환으로 안정화.

---

### ⚠️ 163번 — 현재 시점의 값이 과거 자기 자신의 값들에 의해 설명되는 시계열 모형은? (헷갈림)

**Q.** 현재 시점의 값이 과거 자기 자신의 값들에 의해 설명되는 시계열 모형은? → **정답 ③ 자기회귀(AR) 모형**

**해설** — AR = 과거 자기 자신의 값으로 현재를 설명.

---

### ❌ 164번 — 현재 시점의 값이 과거 백색잡음(오차)들의 선형 결합으로 표현되는 모형은?

**Q.** 현재 시점의 값이 과거 백색잡음(오차)들의 선형 결합으로 표현되는 모형은?
① AR 모형 ② 분해법 ③ ARIMA 모형 ④ MA 모형

| 내 답 | 정답 |
|---|---|
| ③ ARIMA 모형 | **④ MA 모형** |

**해설** — MA = 과거 오차(백색잡음)의 선형 결합.

---

### ❌ 165번 — 평균이 0, 분산이 일정하며 서로 독립인 오차 계열을 무엇이라 하는가?

**Q.** 평균이 0, 분산이 일정하며 서로 독립인 오차 계열을 무엇이라 하는가?
① 백색잡음(White Noise) ② 추세 ③ 계절 변동 ④ 순환 변동

| 내 답 | 정답 |
|---|---|
| ② 추세 | **① 백색잡음(White Noise)** |

**해설** — 백색잡음 = 평균 0, 분산 일정, 자기상관 없는 오차 계열.

---

### ❌ 166번 — ARIMA(p, d, q) 모형에서 d가 의미하는 것은?

**Q.** ARIMA(p, d, q) 모형에서 d가 의미하는 것은?
① 자기회귀 차수 ② 차분 횟수 ③ 이동평균 차수 ④ 계절 주기

| 내 답 | 정답 |
|---|---|
| ① 자기회귀 차수 | **② 차분 횟수** |

**해설** — ARIMA(p,d,q)에서 d = 차분 횟수.

---

### ⚠️ 167번 — ARIMA 모형에서 d=0일 때의 모형은? (헷갈림)

**Q.** ARIMA 모형에서 d=0일 때의 모형은? → **정답 ③ ARMA 모형**

**해설** — d=0이면 차분이 없으므로 ARMA(p,q).

---

### ❌ 168번 — AR(p) 모형의 차수를 식별할 때 주로 사용하는 함수는?

**Q.** AR(p) 모형의 차수를 식별할 때 주로 사용하는 함수는?
① 손실함수 ② 자기상관함수(ACF) ③ 밀도함수 ④ 부분자기상관함수(PACF)

| 내 답 | 정답 |
|---|---|
| ② 자기상관함수(ACF) | **④ 부분자기상관함수(PACF)** |

**해설** — AR 차수 식별 → PACF가 절단되는 시점.

---

### ❌ 169번 — MA(q) 모형의 차수를 식별할 때 주로 사용하는 함수는?

**Q.** MA(q) 모형의 차수를 식별할 때 주로 사용하는 함수는?
① ACF ② PACF ③ 우도함수 ④ 활성화 함수

| 내 답 | 정답 |
|---|---|
| ② PACF | **① ACF** |

**해설** — MA 차수 식별 → ACF가 절단되는 시점.

---

### ⚠️ 171번 — 최근 관측치에 더 큰 가중치를 부여하여 미래를 예측하는 시계열 기법은? (헷갈림)

**Q.** 최근 관측치에 더 큰 가중치를 부여하여 미래를 예측하는 시계열 기법은? → **정답 ③ 지수평활법**

**해설** — 지수평활법 = 최근 데이터에 큰 가중치.

---

### ⚠️ 172번 — 시계열을 추세·계절·불규칙 성분으로 나누어 분석하는 방법은? (헷갈림)

**Q.** 시계열을 추세·계절·불규칙 성분으로 나누어 분석하는 방법은? → **정답 ④ 시계열 분해법**

**해설** — 분해법 = 추세·계절·불규칙 성분 분리.

---

> 🔴 **13문항 중 12문항(92%)이 오답·헷갈림입니다 — 이 오답노트 전체에서 가장 취약한 구간입니다.** 정상성 개념부터 AR·MA·ARIMA 구성, ACF·PACF를 이용한 차수 식별까지 시계열 분석 전체를 처음부터 다시 잡아야 하는 상태입니다.

**개념 정리 — 정상성과 안정화 (160·161·162·165번 동시 해결)**

> **정상성(Stationary)**: **평균·분산이 일정**하고, 공분산이 **시차에만 의존**(시점 자체에는 의존하지 않음)하는 성질.

| 문제 상황 | 안정화 방법 |
|---|---|
| 평균이 일정하지 않음(추세 존재) | **차분(Differencing)** |
| 분산이 변함 | **로그 변환(또는 Box-Cox 변환)** |

> **백색잡음(White Noise)**: 평균 0, 분산 일정, **자기상관 없는** 순수한 오차 계열.

**개념 정리 — AR·MA·ARIMA 구성과 차수 식별 (164·166·168·169·171번 동시 해결)**

| 모형 | 정의 | 차수 식별 |
|---|---|---|
| **AR(p)** | 과거 **자기 자신의 값**으로 현재를 설명 | **PACF가 절단**되는 시점 |
| **MA(q)** | 과거 **오차(백색잡음)**의 선형 결합 | **ACF가 절단**되는 시점 |
| **ARIMA(p,d,q)** | AR + 차분(d) + MA | **d = 차분 횟수** (d=0이면 차분 없는 **ARMA(p,q)**) |
| 지수평활법 | **최근 데이터에 더 큰 가중치** | — |
| 분해법 | **추세·계절·불규칙** 성분으로 분리 | — |

**암기팁** — **"AR은 내 과거값(Auto-Regressive, 자기 자신), MA는 과거 오차(Moving Average, 오차의 평균)"**. 차수 식별은 **"AR은 PACF, MA는 ACF"**로 짝을 맞추세요 — **알파벳 순서(A→P, M→A)로 서로 반대편 짝**이라고 기억하면 헷갈리지 않습니다.

**실수 포인트 ⚠️ 이 영역 전체 재정리 필요** — 12/13이 흔들린다는 것은 개별 문항 암기가 아니라 **시계열의 큰 그림(정상성→안정화→AR/MA 식별)** 자체가 서 있지 않다는 신호입니다. 위 두 표를 **순서대로(정상성 확인 → 안정화 → 차수 식별)** 이야기로 만들어 통째로 복습하는 것을 권합니다.

---

#### 1-12. 다변량 · 비정형 분석

### ⚠️ 173번 — 두 범주형 변수를 교차 집계한 표를 무엇이라 하는가? (헷갈림)

**Q.** 두 범주형 변수를 교차 집계한 표를 무엇이라 하는가? → **정답 ① 분할표(교차표)**

**해설** — 범주형 교차 집계표 = 분할표(교차표).

---

### ❌ 174번 — 표본 수가 적어 카이제곱 검정의 기대도수 조건을 만족하지 못할 때 사용하는 정확 검정은?

**Q.** 표본 수가 적어 카이제곱 검정의 기대도수 조건을 만족하지 못할 때 사용하는 정확 검정은?
① 맥니마 검정 ② 피셔의 정확검정 ③ t검정 ④ F검정

| 내 답 | 정답 |
|---|---|
| ④ F검정 | **② 피셔의 정확검정** |

**해설** — 기대도수 5 미만 셀이 많으면 피셔의 정확검정 사용.

---

### ⚠️ 176번 — 여러 집단을 가장 잘 구분하는 선형 결합식을 찾아 새로운 개체가 어느 집단에 속하는지 예측하는 기법은? (헷갈림)

**Q.** 여러 집단을 가장 잘 구분하는 선형 결합식을 찾아 새로운 개체가 어느 집단에 속하는지 예측하는 기법은? → **정답 ④ 판별분석**

**해설** — 판별분석 = 사전 정의된 집단을 구분하는 판별식 도출.

---

### ⚠️ 177번 — 판별분석과 군집분석의 가장 큰 차이로 옳은 것은? (헷갈림)

**Q.** 판별분석과 군집분석의 가장 큰 차이로 옳은 것은? → **정답 ① 판별분석은 집단이 사전에 정의된 지도학습, 군집분석은 비지도학습이다**

**해설** — 판별분석은 집단이 이미 알려진 지도학습, 군집분석은 비지도학습.

---

### ❌ 178번 — 두 변수 집합 간의 상관관계를 최대로 만드는 선형 결합을 찾는 다변량 기법은?

**Q.** 두 변수 집합 간의 상관관계를 최대로 만드는 선형 결합을 찾는 다변량 기법은?
① 주성분분석 ② 정준상관분석 ③ 요인분석 ④ 군집분석

| 내 답 | 정답 |
|---|---|
| ① 주성분분석 | **② 정준상관분석** |

**해설** — 정준상관분석 = 두 변수 집합 간 상관 최대화 선형결합.

---

### ⚠️ 181번 — 문서를 행으로, 단어를 열로 구성하여 출현 빈도를 표현한 행렬은? (헷갈림)

**Q.** 문서를 행으로, 단어를 열로 구성하여 출현 빈도를 표현한 행렬은? → **정답 ① 문서-단어 행렬(DTM)**

**해설** — DTM(문서-단어 행렬) 또는 전치한 TDM.

---

### ⚠️ 183번 — 문서 집합에 잠재된 주제를 확률적으로 추출하는 대표적인 토픽모델링 기법은? (헷갈림)

**Q.** 문서 집합에 잠재된 주제를 확률적으로 추출하는 대표적인 토픽모델링 기법은? → **정답 ③ LDA(잠재 디리클레 할당)**

**해설** — LDA = 문서를 토픽의 확률 분포로 모델링.

---

### ❌ 185번 — 소셜 네트워크 분석에서 두 노드 사이의 최단 경로에 얼마나 자주 위치하는지를 나타내는 지표는?

**Q.** 소셜 네트워크 분석에서 두 노드 사이의 최단 경로에 얼마나 자주 위치하는지를 나타내는 지표는?
① 매개 중심성 ② 연결정도 중심성 ③ 근접 중심성 ④ 위세 중심성

| 내 답 | 정답 |
|---|---|
| ③ 근접 중심성 | **① 매개 중심성** |

**해설** — 매개 중심성 = 최단 경로상에 위치하는 빈도(중개자 역할).

---

### ⚠️ 186번 — 네트워크에서 한 노드가 다른 모든 노드에 얼마나 가깝게 도달할 수 있는지를 나타내는 지표는? (헷갈림)

**Q.** 네트워크에서 한 노드가 다른 모든 노드에 얼마나 가깝게 도달할 수 있는지를 나타내는 지표는? → **정답 ② 근접 중심성**

**해설** — 근접 중심성 = 다른 노드까지의 평균 거리가 짧을수록 높다.

---

### ⚠️ 188번 — 다음 중 비모수 검정 방법에 해당하지 않는 것은? (헷갈림)

**Q.** 다음 중 비모수 검정 방법에 해당하지 않는 것은? → **정답 ④ 독립표본 t검정**

**해설** — 독립표본 t검정은 모수 검정.

---

### ❌ 190번 — 사용자·상품 간 상호작용 데이터를 이용해 취향이 비슷한 사용자의 선호를 바탕으로 추천하는 방식은?

**Q.** 사용자·상품 간 상호작용 데이터를 이용해 취향이 비슷한 사용자의 선호를 바탕으로 추천하는 방식은?
① 내용 기반 필터링 ② 협업 필터링 ③ 연관규칙 ④ 판별분석

| 내 답 | 정답 |
|---|---|
| ③ 연관규칙 | **② 협업 필터링** |

**해설** — 협업 필터링 = 유사 사용자/아이템의 선호 기반 추천.

---

> ⚠️ **18문항 중 11문항(61%)이 오답·헷갈림입니다.** 판별분석·정준상관분석 같은 다변량 기법, 텍스트 마이닝(DTM·LDA), 네트워크 중심성, 카이제곱 검정의 예외 조건이 섞여 있습니다.

**개념 정리 — 판별분석 vs 군집분석 (176·177번 동시 해결)**

| 구분 | 학습 유형 | 집단 정보 |
|---|---|---|
| **판별분석** | **지도학습** | 집단이 **이미 알려짐** — 판별식으로 **새 데이터를 구분** |
| **군집분석** | 비지도학습 | 집단을 **새로 찾아냄** |

**개념 정리 — 텍스트 마이닝 (181·183번 동시 해결)**

| 개념 | 뜻 |
|---|---|
| **DTM/TDM** | **문서-단어 행렬**(또는 전치한 단어-문서 행렬) |
| **LDA(토픽모델링)** | 문서를 **토픽의 확률 분포**로 모델링 |

**개념 정리 — 네트워크 중심성 (185·186번, 2과목 189·190번과 연결)**

| 중심성 | 의미 |
|---|---|
| **매개 중심성** | **최단 경로 상에 위치**하는 빈도(중개자 역할) |
| **근접 중심성** | 다른 노드까지의 **평균 거리가 짧을수록** 높음 |

> 💡 2과목 오답노트 189·190번(연결정도 중심성·공간적 자기상관)과 함께 **네트워크 중심성 4형제(연결정도·근접·매개·위세)**로 묶어 복습하면 효율적입니다.

**개념 정리 — 기타**

| 개념 | 핵심 |
|---|---|
| **분할표(교차표)** | 범주형 변수의 **교차 집계표** |
| **피셔의 정확검정** | 카이제곱 검정의 **기대도수 5 미만 셀이 많을 때** 대안 |
| **정준상관분석** | **두 변수 집합 간** 상관을 최대화하는 선형결합 |
| **협업 필터링** | **유사 사용자/아이템**의 선호를 기반으로 추천 |
| **독립표본 t검정** | **모수** 검정(비모수 아님 — 2과목 180번 만-휘트니와 대응) |

**실수 포인트 ⚠️** — 178번(정준상관분석)은 "두 변수 집합 간"이라는 조건이 핵심입니다 — 일반 상관분석(변수 하나 대 하나)과 달리 **여러 변수의 묶음끼리** 상관을 봅니다.

---

#### 1-13. 종합 정리 문제

### ⚠️ 191번 — 다음 중 기법과 학습 유형의 연결이 옳지 않은 것은? (헷갈림)

**Q.** 다음 중 기법과 학습 유형의 연결이 옳지 않은 것은? → **정답 ③ K-평균 군집 — 지도학습**

**해설** — K-평균 군집은 비지도학습이다.

---

### ⚠️ 193번 — 다음 중 분류 성능 평가 지표가 아닌 것은? (헷갈림)

**Q.** 다음 중 분류 성능 평가 지표가 아닌 것은? → **정답 ① RMSE**

**해설** — RMSE는 회귀 평가 지표.

---

### ⚠️ 194번 — 앙상블 기법과 특징의 연결로 옳지 않은 것은? (헷갈림)

**Q.** 앙상블 기법과 특징의 연결로 옳지 않은 것은? → **정답 ② 스태킹 — 이전 모델의 오차에 가중치를 부여**

**해설** — 오차에 가중치를 부여하는 것은 부스팅. 스태킹은 메타 모델 결합.

---

### ⚠️ 197번 — 의사결정나무에 대한 설명으로 옳지 않은 것은? (헷갈림)

**Q.** 의사결정나무에 대한 설명으로 옳지 않은 것은? → **정답 ① 지니 지수가 클수록 노드가 순수하다**

**해설** — 지니 지수는 작을수록 순수하다.

---

### ⚠️ 198번 — 딥러닝에 대한 설명으로 옳지 않은 것은? (헷갈림)

**Q.** 딥러닝에 대한 설명으로 옳지 않은 것은? → **정답 ② 드롭아웃은 학습 속도를 높이기 위한 유일한 목적을 갖는다**

**해설** — 드롭아웃의 목적은 과적합 방지이지 속도 향상이 아니다.

---

### ❌ 199번 — 연관규칙 분석에 대한 설명으로 옳지 않은 것은?

**Q.** 연관규칙 분석에 대한 설명으로 옳지 않은 것은?
① 지지도는 전체 거래 대비 동시 발생 비율이다
② 신뢰도는 조건부 확률의 개념이다
③ 향상도는 항상 0과 1 사이의 값을 갖는다
④ 향상도가 1보다 작으면 두 항목은 음의 관계이다

| 내 답 | 정답 |
|---|---|
| ① 지지도는 전체 거래 대비 동시 발생 비율이다 | **③ 향상도는 항상 0과 1 사이의 값을 갖는다** |

**해설** — 향상도는 0 이상의 값을 가지며 1보다 클 수 있다(상한 없음).

---

> ⚠️ **10문항 중 6문항(60%)이 오답·헷갈림입니다.** 앞서 배운 개념들을 다시 묻는 종합 문제 구간으로, 오답보다 헷갈림이 많다는 것은 **개별 지식은 있지만 확신이 부족**하다는 신호입니다.

| 문항 | 핵심 |
|---|---|
| 191 | **K-평균 군집은 비지도학습**(1번 문항과 같은 개념) |
| 193 | **RMSE는 회귀 평가지표**(67번 결정계수와 같은 계열) |
| 194 | **부스팅=오차 가중치 조정, 스태킹=메타 모델 결합**(앙상블 구간과 연결) |
| **199** | **향상도는 0 이상, 1보다 클 수 있음(상한 없음)** — 유일한 확정 오답 |
| 197 | **지니 지수는 작을수록 순수**(68번과 같은 개념) |
| 198 | **드롭아웃의 목적은 과적합 방지**(속도 향상이 아님) |

**실수 포인트 ⚠️ 199번(향상도)** — 향상도(Lift)를 **"0~1 사이"로 착각**하기 쉬운데, 지지도·신뢰도와 달리 **향상도는 상한이 없는 비율(배수) 개념**입니다. **1보다 크면 양의 관계, 1이면 무관, 1보다 작으면 음의 관계**로 해석합니다 — 연관분석 구간(107·108번)과 함께 복습하세요.

**총평** — 종합 문제 구간의 헷갈림 5개는 대부분 **앞선 영역에서 이미 다룬 개념의 재확인**입니다. 위 각 항목이 가리키는 원래 영역(1번↔191번, 68번↔197번 등)을 함께 복습하면 두 배로 효율적입니다.

---

## ⚠️ 오답 패턴 정리

### ① 시계열 분석 — 영역 전체를 처음부터 재정리해야 함 (92% 플래그)

13문항 중 12문항이 흔들렸다는 것은 개별 암기가 아니라 **"정상성 → 안정화 → AR/MA 식별"이라는 큰 그림 자체가 서 있지 않다**는 뜻입니다. 다른 영역보다 압도적으로 우선순위가 높습니다.

### ② 정반대 역할의 짝 개념을 서로 바꿔 씀

27·28번(더빈-왓슨 값의 해석), 32·33번(F검정=전체 vs t검정=개별), 67·68번(분류나무 기준=지니 vs 회귀나무 기준=분산감소) — **두 개념이 정확히 반대 역할을 하는데 자리가 바뀐 경우**가 여러 영역에서 반복됩니다.

> 💡 1·2과목에서도 반복된 패턴입니다. "이 둘은 반대 역할이다"라는 짝을 의식적으로 찾아, **한쪽만 확실히 못 박고 나머지를 자동으로 유도**하는 방식이 효율적입니다.

### ③ 확신 없이 찍어서 맞힌 영역이 오답보다 많음(자신감 부족형)

군집분석(오답2·헷갈림10), 앙상블(오답2·헷갈림6), 종합(오답1·헷갈림6), 로지스틱·분류지표(오답3·헷갈림9), 인공신경망(오답3·헷갈림6) — **다섯 영역 모두 "틀린 것"보다 "찍어서 맞은 것"이 훨씬 많습니다.**

> 💡 이 영역들은 **모르는 게 아니라 확신이 부족한 상태**입니다. 새로 암기하기보다, 각 영역의 개념 표를 눈으로 한 번씩 다시 짚으며 **"왜 이게 답인지"를 소리 내어 설명할 수 있는지** 확인하는 것이 더 빠른 회복 방법입니다.

### ④ 공식 없이 통째로 새로 암기해야 하는 영역

연관분석은 **헷갈림 없이 5문항 전부 확정 오답**입니다 — 찍어서 맞은 것도 없다는 건, 지지도·신뢰도·향상도 공식 자체가 아직 낯설다는 뜻입니다.

> 💡 이런 영역은 오히려 **명확합니다.** 확신 부족형(③)과 달리 "모른다"는 게 분명하니, 공식 3개(지지도·신뢰도·향상도)만 새로 정확히 암기하면 빠르게 해결됩니다.

---

## 🎯 핵심 요약 카드

| # | 한 줄 정리 |
|---|---|
| 1 | **검증용=하이퍼파라미터 튜닝 / 평가용=최종 성능 보고** |
| 2 | **홀드아웃=1회 분할(분산 큼) / K-fold=K회 / LOOCV=K=n / 부트스트랩=복원추출** |
| 3 | **과적합=낮은 편향+높은 분산**, 모델이 복잡할수록 이 방향 |
| 4 | 회귀 정규성 가정은 **오차항(잔차)**의 이야기, 원변수 아님 |
| 5 | **DW≈2=자기상관 없음** / **F검정=모형 전체 / t검정=개별 계수** |
| 6 | **VIF≥10(엄격히 5)=다중공선성** / **AIC·BIC는 작을수록 좋음** |
| 7 | **정확도=(TP+TN)/전체 / 정밀도=TP/(TP+FP)** — 분모가 다름 |
| 8 | **임계값↓ → 재현율↑ 정밀도↓** (트레이드오프) |
| 9 | **Logit=ln(Odds) / exp(β)=오즈비 / 로지스틱=MLE 추정** |
| 10 | **AUC 0.5 = 랜덤(동전 던지기)**, 1에 가까울수록 우수 |
| 11 | **CART=이진분리(지니) / C4.5=다지(엔트로피) / CHAID=다지(카이제곱)** |
| 12 | **정보이득=부모엔트로피−자식가중엔트로피** / 회귀나무=분산감소량 |
| 13 | **tanh=−1~1(0중심) / 소프트맥스=확률분포 합1** |
| 14 | **조기종료는 "검증" 오차 기준** (학습 오차 아님) |
| 15 | **커널트릭=실제 사상 없이 내적만 계산** |
| 16 | **KNN=게으른 학습**(사전 모델 없음), K작음=과적합 K큼=과소적합 |
| 17 | **지지도=P(A∩B) / 신뢰도=P(B\|A) / 향상도=상한 없음(0 이상)** |
| 18 | **Apriori=후보생성+반복스캔(느림) / FP-Growth=트리로 후보없이(빠름)** |
| 19 | **최단연결법=사슬효과 / 와드연결법=오차제곱합 최소화** |
| 20 | **K-medoids=실제 관측치 중심(이상값에 강건)** |
| 21 | **GMM=정규분포 혼합+EM추정 / SOM=코호넨맵, 2차원 격자** |
| 22 | **마할라노비스=공분산 반영 / 코사인=각도(문서) / 자카드=교집합÷합집합** |
| 23 | **배깅=병렬(분산↓) / 부스팅=순차(편향↓)** |
| 24 | **랜덤포레스트=배깅+변수 무작위선택** / **GBM=잔차 학습** |
| 25 | **OOB = 부트스트랩에서 안 뽑힌 약 36.8%**로 검증 |
| 26 | **LSTM=게이트 3개 / GRU=게이트 2개**(LSTM 경량화) |
| 27 | **오토인코더=비지도(레이블 없이 복원)** |
| 28 | **분류 손실함수=교차엔트로피 / 회귀=MSE** |
| 29 | **정상성=평균·분산 일정, 공분산은 시차에만 의존** |
| 30 | **평균 불안정→차분 / 분산 불안정→로그(Box-Cox) 변환** |
| 31 | **AR=PACF로 식별 / MA=ACF로 식별** (알파벳 반대 짝) |
| 32 | **ARIMA의 d=차분 횟수**, d=0이면 ARMA |
| 33 | **판별분석=지도(집단 앎) / 군집분석=비지도(집단 찾음)** |
| 34 | **DTM/TDM=문서-단어행렬 / LDA=토픽의 확률분포** |
| 35 | **매개중심성=최단경로 위 위치 / 근접중심성=평균거리 짧음** |
| 36 | **피셔의 정확검정=기대도수 5 미만 셀 많을 때** |
| 37 | **정준상관분석=두 "변수 집합" 간 상관** |
| 38 | **드롭아웃 목적=과적합 방지**(속도 아님) |

---

## 📝 복습 체크리스트

> 문항 수가 많아(108문항) **영역 단위로 묶었습니다.** 영역명을 클릭하듯 훑고, 그 안에서 특히 헷갈렸던 번호만 따로 표시해 두세요.

| 영역 | 오답·헷갈림 문항 | D+1 | D+3 | D+7 | 졸업 |
|---|---|---|---|---|---|
| 분석모형 설계·검증 | 4·5·7·8·9·11·12·16·17 | ☐ | ☐ | ☐ | ☐ |
| 회귀분석 | 22·25·26·27·28·32·33·35·37·38 | ☐ | ☐ | ☐ | ☐ |
| 로지스틱·분류 평가지표 | 43·45·46·47·48·49·50·56·57·58·59 | ☐ | ☐ | ☐ | ☐ |
| 의사결정나무 | 67·68·69·70·71·72·73 | ☐ | ☐ | ☐ | ☐ |
| 인공신경망 | 78·80·82·84·89·90·92·93·95 | ☐ | ☐ | ☐ | ☐ |
| SVM·KNN·나이브베이즈 | 99·102·103 | ☐ | ☐ | ☐ | ☐ |
| 연관분석 | 107·108·111·112·113 | ☐ | ☐ | ☐ | ☐ |
| 군집분석 | 120·121·122·123·124·125·128·129·130·133·134·135 | ☐ | ☐ | ☐ | ☐ |
| 앙상블 | 136·139·141·142·144·145·146·147 | ☐ | ☐ | ☐ | ☐ |
| 딥러닝 | 152·153·154·156·158 | ☐ | ☐ | ☐ | ☐ |
| 시계열 분석 | 160·161·162·163·164·165·166·167·168·169·171·172 | ☐ | ☐ | ☐ | ☐ |
| 다변량·비정형 분석 | 173·174·176·177·178·181·183·185·186·188·190 | ☐ | ☐ | ☐ | ☐ |
| 종합 | 191·193·194·197·198·199 | ☐ | ☐ | ☐ | ☐ |

---

## ✅ 남은 과제

1. **🔴 시계열 분석 영역 전면 재학습** — 13문항 중 12문항이 흔들린 3과목 최대 위험 구간입니다. "정상성 확인 → 안정화(차분/로그) → AR·MA 식별(PACF/ACF)"이라는 큰 흐름부터 다시 잡으세요.
2. **🔴 연관분석 공식 3개 재암기** — 지지도·신뢰도·향상도, 헷갈림 없이 전부 확정 오답입니다. 공식만 정확히 외우면 바로 회복되는 영역입니다.
3. **🟡 확신 부족형 5개 영역 자신감 점검** — 군집분석·앙상블·종합·로지스틱/분류지표·인공신경망은 헷갈림이 오답보다 훨씬 많습니다. 각 영역의 개념 표를 다시 훑고 "왜 이게 답인지" 스스로 설명해보세요.
4. **🟡 "반대 역할 짝" 개념 재정리** — DW 해석, F검정↔t검정, 분류나무↔회귀나무 분리기준처럼 정반대 역할의 개념이 자리를 바꾼 경우가 많습니다. 한쪽만 확실히 못 박는 방식으로 접근하세요.
5. **3과목 완주** — 200제를 모두 마쳤습니다. 정답률 76%(헷갈림 포함 45%)로, **1·2과목보다 보완이 시급한 상태**입니다. 시계열 분석과 연관분석부터 우선 정리한 뒤, 나머지 확신 부족형 영역을 순서대로 복습하시길 권합니다.

## 📖 부록 — PART C 시험장 직전 암기노트

> 3과목 예상문제 200제의 **PART C**를 오답노트에 옮겨 둔 것입니다. **시험 직전에는 이 부록만 15분 훑으면 됩니다.**

### 1️⃣ 모형 설계 & 검증

- **구축 절차**: 요건 정의 → 모델링 → 검증·테스트 → 적용
- **데이터 분할**: 학습(파라미터) / 검증(**하이퍼파라미터·모델 선택**) / 평가(최종 성능)
- **검증 기법**: 홀드아웃 · **K-fold** · LOOCV(K=n) · 부트스트랩(복원추출)

**과적합 ↔ 과소적합**
| | 편향 | 분산 | 특징 |
|---|---|---|---|
| **과적합** | 낮음 | **높음** | 학습 성능↑, 일반화↓ |
| **과소적합** | **높음** | 낮음 | 학습조차 안 됨 |

> 💡 **모델 복잡도↑ → 편향↓ 분산↑** (트레이드오프)
> 방지책: 규제(L1/L2) · 드롭아웃 · 조기 종료 · 데이터 확대 · 교차검증

---

### 2️⃣ 회귀분석

**가정 4가지 — "선·독·등·정"** (선형성 · 독립성 · 등분산성 · **오차의** 정규성)

| 진단 대상 | 지표 |
|---|---|
| 자기상관(독립성) | **더빈-왓슨** (2 근처 = 정상) |
| 다중공선성 | **VIF ≥ 10** |
| 모형 전체 유의성 | **F 검정** |
| 개별 계수 유의성 | **t 검정** |
| 설명력 | R² / **수정 R²**(변수 수 보정) |
| 모형 비교 | **AIC · BIC (작을수록 좋음)** |

**규제 회귀**
- **릿지(L2)**: 계수 축소, 0은 안 됨
- **라쏘(L1)**: 계수 0 가능 → **변수 선택**
- 엘라스틱넷: L1 + L2

**회귀 평가지표**: MSE · **RMSE**(원 단위) · **MAE**(이상값에 둔감) · MAPE(%) · R²

---

### 3️⃣ 분류 & 평가지표

**로지스틱 회귀**
- **오즈 = p/(1−p)** → **로짓 = ln(오즈)** → 시그모이드로 역변환
- **exp(β) = 오즈비**, 계수 추정은 **최대우도추정(MLE)**

**혼동행렬 지표**
| 지표 | 식 | 의미 |
|---|---|---|
| 정확도 | (TP+TN)/전체 | 전체 정답률 |
| **정밀도** | TP/(TP+**FP**) | 예측 P 중 실제 P |
| **재현율(민감도)** | TP/(TP+**FN**) | 실제 P 중 맞춘 것 |
| 특이도 | TN/(TN+FP) | 실제 N 중 맞춘 것 |
| **F1** | 정밀도·재현율의 **조화평균** | 불균형 데이터용 |

- **ROC**: x = 1−특이도(FPR), y = 민감도(TPR) / **AUC 1에 가까울수록 우수, 0.5 = 무작위**
- **임계값↓ → 재현율↑ 정밀도↓**

---

### 4️⃣ 의사결정나무 & SVM & KNN

**분리 기준**
| 알고리즘 | 기준 | 분리 |
|---|---|---|
| **CART** | 지니 지수 | **이진** |
| **C4.5(C5.0)** | 엔트로피·정보이득비 | 다지 |
| **CHAID** | **카이제곱** | 다지 |
| 회귀나무 | **분산 감소(F 통계량)** | — |

> ⚠️ **지니·엔트로피는 작을수록 순수**
> 정지 규칙(사전) ↔ **가지치기(사후, 과적합 방지)**

**SVM**: 초평면 · **마진 최대화** · **서포트 벡터** · **커널 트릭**(비선형) · 소프트 마진(오분류 허용)
**KNN**: **게으른 학습**, K 작으면 과적합 / K 크면 과소적합
**나이브베이즈**: 변수 독립 가정, 텍스트 분류에 강함

---

### 5️⃣ 연관분석 & 군집분석

**연관규칙 3지표**
| 지표 | 식 |
|---|---|
| 지지도 | P(A∩B) |
| 신뢰도 | P(B\|A) |
| **향상도** | 신뢰도 ÷ P(B) |

> **Lift = 1 독립 / > 1 양의 관계 / < 1 음의 관계**
> **Apriori**(후보 생성, 느림) ↔ **FP-Growth**(FP-Tree, 빠름)

**군집 기법**
| 기법 | 핵심 |
|---|---|
| **K-means** | K 사전 지정, 이상값 민감, 빠름 |
| K-medoids | 중심으로 실제 관측치 사용(이상값 강건) |
| **계층적** | 덴드로그램, K 불필요 |
| **DBSCAN** | 밀도 기반, **잡음점 식별**, K 불필요 |
| 혼합분포(GMM) | **EM 알고리즘**으로 추정 |
| SOM | 2차원 격자 사상 |

**연결법**: 최단(**사슬 효과**) · 최장 · 평균 · 중심 · **와드(오차제곱합 최소)**
**거리**: 유클리드(직선) · 맨해튼(절댓값 합) · **마할라노비스(공분산 반영)** · **코사인(각도, 문서)** · **자카드(교/합)**
**K 결정**: **엘보우** · **실루엣 계수(1에 가까울수록 좋음)**

---

### 6️⃣ 앙상블

| 기법 | 학습 방식 | 효과 |
|---|---|---|
| **배깅** | **병렬** (부트스트랩) | **분산↓** |
| **부스팅** | **순차** (오차 가중) | **편향↓** |
| 보팅 | 다수결·평균 | 결합 |
| 스태킹 | 메타 모델 학습 | 결합 |

- **랜덤포레스트 = 배깅 + 변수 무작위 선택**, **OOB**로 평가
- 부스팅 계열: **AdaBoost**(가중치) → **GBM**(잔차 학습) → **XGBoost**(규제+병렬)

---

### 7️⃣ 딥러닝

**활성화 함수**
| 함수 | 출력 범위 | 용도 |
|---|---|---|
| 시그모이드 | 0~1 | **이진 분류 출력층** |
| tanh | −1~1 | 은닉층 |
| **ReLU** | 0~∞ | **기울기 소실 완화** |
| **소프트맥스** | 합=1 | **다중 분류 출력층** |

- **역전파 + 경사하강법**으로 학습 / 학습률 큼 → 발산, 작음 → 느림
- **에포크**(전체 1회) / **배치**(묶음) / 이터레이션
- 과적합 방지: **드롭아웃 · 조기 종료 · 배치 정규화(안정화)**
- 옵티마이저: SGD → 모멘텀 → **Adam(모멘텀 + 적응 학습률)**
- 손실함수: 분류 = **교차 엔트로피** / 회귀 = **MSE**

**구조별 용도**
| 모델 | 용도 |
|---|---|
| **CNN** | 이미지 (합성곱·풀링·패딩) |
| **RNN** | 순차·시계열 |
| **LSTM** | 장기 의존성 해결 (게이트 3개) |
| GRU | LSTM 단순화 (게이트 2개) |
| **GAN** | 생성자 vs 판별자 |
| 오토인코더 | 압축·복원, 차원 축소 |

---

### 8️⃣ 시계열 & 고급 기법

- **정상성**: 평균·분산 일정 + 공분산은 **시차에만** 의존
- **평균 불안정 → 차분 / 분산 불안정 → 로그 변환**
- **AR**(과거 자기 값) / **MA**(과거 오차) / ARMA / **ARIMA(p, d, q)** — **d = 차분 횟수**
- **차수 식별: AR → PACF, MA → ACF** (⭐ 자주 출제)
- 시계열 구성: 추세 · 순환 · 계절 · 불규칙
- **판별분석(지도) ↔ 군집분석(비지도)**
- 정준상관분석(두 변수 집합), MANOVA(종속변수 2개↑)
- 텍스트: DTM/TDM · TF-IDF · 감성분석 · **LDA(토픽모델링)**
- SNA 중심성: **연결정도**(직접 연결 수) · **근접**(도달 거리) · **매개**(최단경로 중개)

---

### ⚠️ 최다 함정 TOP 10

1. **정규성 가정의 대상은 오차항** (독립변수 아님)
2. **VIF 클수록 다중공선성 높음** (낮음 아님)
3. **지니·엔트로피는 작을수록 순수**
4. **배깅 = 병렬·분산↓ / 부스팅 = 순차·편향↓**
5. **AR → PACF, MA → ACF** (거꾸로 낚음)
6. **라쏘(L1)만 계수를 0으로** 만든다
7. **재현율 = FN을 줄이는 지표** (정밀도는 FP)
8. **AUC 0.5 = 무작위** (0이 최악 아님)
9. **혼합분포 군집은 EM 알고리즘** (경사하강법 아님)
10. **향상도는 상한이 없다** (0~1 아님)

---

*끝. 시험 직전에는 PART C만 15분 훑으면 됩니다.*
