# 📗 빅데이터분석기사 필기 — 2과목 오답노트 (데이터 전처리 · 데이터 탐색)

> **2과목(빅데이터 탐색) 200제 중 1~124번을 풀고, 틀리거나 헷갈린 48문항을 개념별로 정리했습니다.** 1과목 오답노트와 같은 방식으로 회차 구분 없이 개념 단위로 구성합니다.
>
> **표기**: ❌ 틀림 · ⚠️ 찍어서 맞음/헷갈림 · ✅ 졸업
> **복습 주기**: D+1 / D+3 / D+7 세 번 다시 봅니다. 세 번 다 맞히면 ✅ 표시 후 졸업.
> **범위 안내**: 1~50번은 **데이터 전처리**, 56~100번은 **데이터 탐색(EDA·상관·시각화)**, 101번부터 **통계기법(확률분포)** 영역이 시작됐습니다. 나머지 출제범위(표본추출·추정·검정)는 아직 포함되어 있지 않습니다. 진행하시면 이어서 추가됩니다.

---

## 🔍 전문가 검토 결과

1과목 오답노트와 동일한 절차로 48문항의 정답·핵심 정의를 재검토했습니다. (실제 외부 인력이 아니라 5가지 관점으로 나누어 스스로 재점검한 절차입니다.)

| 관점 | 중점 점검 사항 | 결과 |
|---|---|---|
| **출제위원 시각** | 정답이 표준 교재(전처리·EDA·확률분포 파트) 기준과 일치하는가 | 48문항 전수 확인, **불일치 없음** |
| **학원강사 시각** | 필터·래퍼·임베디드, PCA·LDA, EDA 4대 주제, 이산·연속분포 등 구분 키워드가 정확한가 | 표현 4곳 보강 (아래 참고) |
| **통계·ML 전문가 시각** | 다중대치법 절차, 왜도 방향, 정규화 범위 등 수치·정의가 실무와 어긋나지 않는가 | 뉴앙스 주석 2건 추가 |
| **교수 시각** | PCA↔요인분석, 필터↔래퍼↔임베디드 등 개념 간 경계의 논리적 정합성 | 정합성 문제 없음 |
| **현직 데이터 분석가 시각** | 스케일링·인코딩 등 실무에서 실제로 쓰는 기준과 부합하는가 | 실무 단순화 주석 1건 |

**보강한 4곳**
1. **다중 대치법 3단계(대치→분석→결합)** — 통계학 원문 용어(Rubin's Multiple Imputation: Imputation→Analysis→Pooling)와 대조해 "결합(Combination)=Pooling"임을 명확히 표기
2. **로그 변환이 양의 왜도를 완화하는 이유** — "큰 값을 강하게 압축한다"는 설명에 왜 압축이 왜도를 줄이는지 근거를 보강
3. **EDA 4대 주제(저항성·잔차해석·자료재표현·현시성)와 '인과성'의 관계** — 인과성이 왜 EDA 용어가 아니라 별개의 통계 분야(인과추론) 용어인지 근거를 명시
4. **카이제곱 분포가 "자유도에 따라 모양이 달라진다"는 설명** — 자유도가 커질수록 정규분포에 근사한다는 근거를 명시(124번)

**뉴앙스 주석**
- **임베디드 기법에 라쏘·릿지·엘라스틱넷을 모두 포함**하는 것은 표준 교재 분류입니다. 다만 **릿지(L2)는 계수를 0으로 만들지 못해 엄밀히는 변수를 "선택"하지 않고 "축소"만** 합니다. 그럼에도 임베디드로 분류하는 이유는 **학습 과정 자체에 규제가 내장되어 있다는 분류 기준** 때문이며, "변수 선택 효과가 있는가"와는 별개의 기준입니다. 30번 문항에서 헷갈리기 쉬운 지점입니다.
- **PCA는 비지도, LDA는 지도**라는 구분은 시험에서 항상 옳지만, 현업에서는 **LDA도 차원 축소 목적(분류 성능 향상)으로 전처리에 함께 활용**되는 경우가 많습니다. 시험 답안 기준으로는 문제없습니다.

**결론 — 48문항의 정답·핵심 정의에서 사실 오류는 발견되지 않았습니다.**

---

## 📊 전체 현황

**현재까지(1~124번) 오답 37 · 헷갈림 11 · 정답률 70%(87/124)** — 헷갈린 11문항까지 실전 오답으로 가정하면 **61%**로, 1과목에서 언급한 합격 평균 커트라인(60%)을 겨우 웃도는 수준입니다. 여유가 거의 없으니 아래 취약 구간부터 보완하세요.

| 소단원 | 범위 | 오답 | 헷갈림 | 상태 |
|---|---|---|---|---|
| 데이터 정제·오류 유형 | 1~5 | 1 | 0 | 🟢 양호 |
| 결측값 처리 | 6~15 | 2 | 1 | 🟡 보완 |
| **변수 선택 기법** | 21~30 | **4** | **1** | 🔴 최우선 |
| **차원 축소 기법** | 31~40 | **4** | 0 | 🔴 최우선 |
| 파생변수·데이터 변환 | 41~45 | 2 | 2 | 🟡 보완 |
| 스케일링·클래스 불균형 | 46~55 | 2 | 0 | 🟡 보완 |
| **EDA 개념·4대 주제** | 56~60 | **3** | **1** | 🔴 최우선 |
| **대푯값·산포도(평균 종류·CV)** | 61~70 | **4** | **2** | 🔴 최우선 |
| 첨도 | 71~75 | 1 | 0 | 🟢 양호 |
| 그래프 해석 | 76~80 | 2 | 1 | 🟡 보완 |
| **공분산·상관계수** | 81~85 | **3** | 0 | 🔴 최우선 |
| 공간 시각화·시각화 절차 | 91~100 | 2 | 0 | 🟡 보완 |
| 확률의 기초 | 101~110 | 2 | 1 | 🟡 보완 |
| **확률분포 총정리** | 111~120 | **3** | **2** | 🔴 최우선 |
| 카이제곱 분포 | 121~125 | 1 | 0 | 🟢 양호 |

**🔴 최우선 보완 구간 4곳**
- **21~40번(변수 선택 + 차원 축소)** — 20문항 중 9문항이 오답·헷갈림. 1과목의 "63~94번 하둡 구간"과 같은 성격의 최대 취약 구간입니다.
- **61~70번(평균의 종류 + 산포도)** — **10문항 중 6문항**이 오답·헷갈림(60%). 기하평균·조화평균의 짝이 뒤바뀌고(63·64), 변동계수 구간(67~69)이 통째로 흔들렸습니다.
- **81~85번(공분산·상관계수)** — **5문항 중 3문항**이 오답(60%). "상관계수 0 = 독립 아님"이라는 최다 함정이 여기서도 반복됐습니다.
- **111~120번(확률분포)** — **10문항 중 5문항**이 오답·헷갈림(50%). 이산분포(기하·초기하)와 연속분포(균등·지수) 정의가 섞여 있습니다.

**🟢 양호 구간도 있습니다** — 첨도(71~75)·카이제곱 정의(121~125)는 각 1문항만 걸려 상대적으로 안정적입니다.

---

## PART 1. 데이터 전처리

#### 1-1. 데이터 정제 · 오류 유형

### ❌ 5번 — 무작위 측정 오차

**Q.** 측정 과정에서 무작위로 발생하여 값의 변동을 일으키는 오류를 무엇이라 하는가?
① 결측값 ② 이상값 ③ 노이즈 ④ 파생변수

| 내 답 | 정답 |
|---|---|
| ② 이상값 | **③ 노이즈** |

**해설(PART B)** — 노이즈 = 측정 과정의 무작위 오차로 인한 변동.

**개념 정리 — 데이터 오류 4유형 구분**

| 유형 | 정의 | 원인 성격 |
|---|---|---|
| **결측값** | 값이 **없음**(NULL) | 미수집·미응답 |
| **이상값** | 범위를 크게 **벗어난** 값 | 실제 현상 또는 오류 둘 다 가능 |
| **노이즈** | **무작위**로 생기는 값의 흔들림 | 측정 장비·환경의 무작위 오차 |
| 파생변수 | (오류 아님) 기존 변수를 가공해 새로 만든 변수 | — |

**암기팁** — **"노이즈는 무작위(Random), 이상값은 극단(Extreme)"** — 성격이 다릅니다. 노이즈는 매번 조금씩 흔들리는 **잡음**이고, 이상값은 그 결과로 튀어나온 **극단적인 값 하나**입니다.

**실수 포인트 ⚠️** — 이상값과 노이즈를 같은 것으로 보기 쉽지만, **노이즈가 누적되면 이상값처럼 보일 수 있어도 정의 자체는 다릅니다.** 문제에 **"무작위"·"측정 과정"**이라는 단어가 있으면 노이즈, **"범위를 벗어난 값 자체"**를 가리키면 이상값입니다.

---

#### 1-2. 결측값 처리

### ❌ 10번 — 단순 대치법이 아닌 것

**Q.** 결측값 처리 방법 중 '단순 대치법'에 해당하지 않는 것은?
① 완전 분석법 ② 평균 대치법 ③ 단순 확률 대치법 ④ 다중 대치법

| 내 답 | 정답 |
|---|---|
| ① 완전 분석법 | **④ 다중 대치법** |

**해설(PART B)** — 다중 대치법은 단순 대치법과 구분되는 별도 범주.

**개념 정리**

| 분류 | 방법 |
|---|---|
| **단순 대치법** | **완전 분석법** · 평균 대치법 · 단순 확률 대치법(핫덱·콜드덱·회귀 대치 등) |
| **다중 대치법** | 대치(여러 벌 생성) → 분석 → 결합 |

**암기팁** — **"단순은 한 번, 다중은 여러 번"** — '다중'은 '단순'의 반대말이므로 단순 대치법에 속할 수 없습니다.

**실수 포인트 ⚠️** — ① 완전 분석법은 **결측 행을 통째로 버리는 방법**이라 "대치를 안 하는데 왜 대치법인가?" 싶어 고르기 쉽지만, **분류상 단순 대치법에 포함**됩니다. 보기 4개 중 **"완전분석·평균·단순확률"은 전부 한 번에 처리, '다중'만 여러 번 반복**한다는 결을 보면 답이 보입니다.

---

### ❌ 13번 — 다중 대치법의 3단계 순서

**Q.** 다중 대치법(Multiple Imputation)의 3단계 순서로 옳은 것은?
① 대치(Imputation) → 분석(Analysis) → 결합(Combination)
② 분석 → 대치 → 결합
③ 결합 → 대치 → 분석
④ 대치 → 결합 → 분석

| 내 답 | 정답 |
|---|---|
| ② 분석 → 대치 → 결합 | **① 대치 → 분석 → 결합** |

**해설(PART B)** — 다중 대치 = 대치(여러 벌 생성) → 각각 분석 → 결과 결합.

**개념 정리 — 다중 대치법(Rubin's MI) 3단계**

| 단계 | 영문 | 하는 일 |
|---|---|---|
| ① **대치** | Imputation | 결측값을 **여러 번(예: 5벌)** 서로 다르게 채워 완성된 데이터셋을 여러 개 만듦 |
| ② **분석** | Analysis | 각 데이터셋을 **따로따로** 분석(통계량 산출) |
| ③ **결합** | Combination(Pooling) | 여러 분석 결과를 **하나로 합쳐** 최종 추정치·표준오차 산출 |

**암기팁** — **"먼저 채워야(대치) 분석할 수 있고, 여러 번 분석한 걸 마지막에 합친다(결합)"** — 순서가 상식과 같습니다. **채우기 전에 분석할 수 없고, 합치기 전에 각각 분석부터 해야** 합니다.

**실수 포인트 ⚠️** — ②(분석→대치→결합)를 고르신 건 **"분석이 먼저 나와야 할 것 같다"**는 직관 때문일 수 있습니다. 하지만 **아직 채우지 않은 결측 데이터는 분석할 수 없습니다.** '대치'라는 단어 자체가 **"빈 곳을 채운다"**는 뜻이므로, 채우는 일이 항상 맨 앞입니다.

---

### ⚠️ 14번 — 핫덱 대체 (헷갈림)

**Q.** 결측값을 동일한 조사 내에서 유사한 특성을 가진 다른 응답자의 값으로 대체하는 방법은?
① 콜드덱(Cold-deck) 대체 ② 핫덱(Hot-deck) 대체 ③ 평균 대치 ④ 회귀 대치

→ **정답 ② 핫덱(Hot-deck) 대체**

**해설(PART B)** — 핫덱 = 같은 조사 내 유사 응답자 값 사용. 콜드덱은 외부/과거 자료 사용.

**개념 정리 — 핫덱 vs 콜드덱**

| 구분 | 값을 가져오는 곳 |
|---|---|
| **핫덱(Hot-deck)** | **같은 조사(현재 데이터) 안**의 유사 응답자 |
| **콜드덱(Cold-deck)** | **외부 자료·과거 조사** 등 다른 소스 |

**암기팁** — **"뜨거운(Hot) 건 지금 막 만든 것(현재 조사 내부), 차가운(Cold) 건 예전에 만들어 둔 것(외부·과거 자료)"** — Hot이 '현재·내부', Cold가 '외부·과거'라는 온도 비유로 기억하면 헷갈리지 않습니다.

**실수 포인트** — 핫덱·콜드덱은 이름만 봐서는 구분이 안 되는 대표적인 함정 용어입니다. **"동일한 조사 내에서"**라는 문구가 있으면 핫덱, **"외부 데이터베이스"·"과거 자료"**라는 문구가 있으면 콜드덱입니다.

---

#### 1-3. 변수 선택 기법 — 필터 · 래퍼 · 임베디드 ⭐3자 순환 오답⭐

> ⚠️ **22·23·24번의 오답이 한 칸씩 밀려 있습니다.** 22번(필터가 정답)에 래퍼를 쓰고, 23번(래퍼가 정답)에 임베디드를 쓰고, 24번(임베디드가 정답)에는 아예 다른 범주(차원 축소 기법)를 썼습니다. **개념 자체는 알고 있지만, "필터→래퍼→임베디드" 세 이름의 자리가 한 칸씩 밀려서 저장된 상태**입니다.

### ❌ 22번 — 통계 척도로 모델과 독립적으로 선택

**Q.** 통계적 척도(상관계수, 카이제곱 등)를 이용해 모델과 독립적으로 변수를 선택하는 기법은?
① 필터 기법 ② 래퍼 기법 ③ 임베디드 기법 ④ 앙상블 기법

| 내 답 | 정답 |
|---|---|
| ② 래퍼 기법 | **① 필터 기법** |

**해설(PART B)** — 필터 = 모델과 독립적으로 통계 척도만으로 선별(속도 빠름).

---

### ❌ 23번 — 부분집합을 반복 적용해 성능 평가

**Q.** 변수의 부분집합을 반복적으로 모델에 적용해 성능을 평가하며 최적 조합을 찾는 기법은?
① 필터 기법 ② 정규화 기법 ③ 임베디드 기법 ④ 래퍼 기법

| 내 답 | 정답 |
|---|---|
| ③ 임베디드 기법 | **④ 래퍼 기법** |

**해설(PART B)** — 래퍼 = 모델 성능을 평가 기준으로 변수 조합 탐색(성능 좋으나 느림).

---

### ❌ 24번 — 학습 과정 자체에 내장된 변수 선택

**Q.** 모델 학습 과정 자체에 변수 선택 기능이 포함된 기법은?
① 필터 기법 ② 래퍼 기법 ③ 임베디드 기법 ④ 차원 축소 기법

| 내 답 | 정답 |
|---|---|
| ④ 차원 축소 기법 | **③ 임베디드 기법** |

**해설(PART B)** — 임베디드 = 학습 알고리즘 내부에 변수 선택이 내장.

**개념 정리 — 변수 선택 3기법 (22·23·24번 동시 해결)**

| 기법 | 모델과의 관계 | 판단 기준 | 속도 | 대표 예 |
|---|---|---|---|---|
| **필터(Filter)** | **독립적**(모델 없이 먼저 거름) | **통계 척도**(상관계수·카이제곱) | **빠름** | 상관계수 필터링 |
| **래퍼(Wrapper)** | 모델에 **직접 넣어** 반복 평가 | **모델 성능**(정확도 등) | 느림 | 전진선택·후진제거·단계적선택 |
| **임베디드(Embedded)** | 학습 과정에 **내장** | **알고리즘 자체의 규제** | 중간 | 라쏘·릿지·엘라스틱넷 |

**암기팁 — 세 기법을 "언제 변수를 거르는가"의 순서로 기억하세요**

> **필터 = 모델 학습 "전에" 미리 거름** (모델 구경도 안 함 → 통계만 보고 판단 → 가장 빠름)
> **래퍼 = 모델을 "여러 번 돌려보며" 거름** (모델에 넣었다 뺐다 반복 → 가장 느림)
> **임베디드 = 모델을 "한 번 학습하는 도중에" 저절로 거름** (학습과 선택이 동시에 일어남)
>
> **한글 뜻으로 순서를 잡으면 헷갈리지 않습니다: "필터(거르개)는 모델 앞", "래퍼(감싸개)는 모델을 감싸고 반복", "임베디드(내장)는 모델 안에 박혀있음".**

**실수 포인트 ⚠️ 순환 오답 교정법**
- 세 문제가 **한 칸씩 밀려 있다는 것 자체가 힌트**입니다. 세 이름이 **"거르는 시점" 순서(전 → 반복 → 내장)**로 나란히 있다는 것만 확정하면, 나머지는 문제 속 키워드로 자동 정리됩니다.
- **"통계 척도" 단어 = 필터**, **"반복 평가·성능" 단어 = 래퍼**, **"학습 과정 자체에 포함" 단어 = 임베디드**. 이 세 키워드만 문제에서 찾으면 됩니다.
- 24번처럼 **"차원 축소"가 보기로 나오면 무조건 오답**입니다. 차원 축소(PCA 등)는 **변수를 선택하는 게 아니라 새로운 축을 만드는 것**이라 이 3기법과는 아예 다른 카테고리입니다 (25번 참고).

---

### ⚠️ 25번 — 래퍼 기법의 탐색 방법이 아닌 것 (헷갈림)

**Q.** 래퍼(Wrapper) 기법의 탐색 방법에 해당하지 않는 것은?
① 전진 선택법 ② 후진 제거법 ③ 단계적 선택법 ④ 주성분 분석

→ **정답 ④ 주성분 분석**

**해설(PART B)** — PCA는 차원 축소(변수 추출) 기법으로, 변수 선택 탐색법이 아니다.

**개념 정리** — 래퍼 기법의 3가지 탐색 방법: **전진 선택법**(하나씩 추가) · **후진 제거법**(하나씩 제거) · **단계적 선택법**(추가+제거 병행)

**실수 포인트** — 24번과 같은 함정입니다. **PCA(주성분분석)는 변수를 "고르는" 기법이 아니라 변수들을 조합해 "새 축을 만드는" 기법**입니다. 변수 선택(있는 변수 중 고르기) vs 변수 추출/차원 축소(변수를 조합해 새로 만들기)는 완전히 다른 카테고리이며, 이 구분이 22~40번 구간 전체를 관통하는 핵심 축입니다.

---

### ❌ 29번 — 임베디드 기법이 아닌 것

**Q.** 임베디드 기법에 해당하지 않는 것은?
① 라쏘(LASSO) ② 릿지(Ridge) ③ 엘라스틱넷(Elastic Net) ④ 상관계수 필터링

| 내 답 | 정답 |
|---|---|
| ③ 엘라스틱넷 | **④ 상관계수 필터링** |

**해설(PART B)** — 상관계수 필터링은 필터 기법.

**실수 포인트** — 이름에 **"필터링"**이 그대로 들어 있습니다. 라쏘·릿지·엘라스틱넷은 전부 **규제(regularization) 기반 임베디드 기법**이고, 상관계수 필터링만 **필터 기법**입니다. **이름 안에 답이 있는 문제**였습니다.

---

### ❌ 30번 — 라쏘와 릿지의 차이

**Q.** 라쏘(LASSO)와 릿지(Ridge)의 차이로 옳은 것은?
① 라쏘는 L1 규제로 계수를 0으로 만들어 변수 선택 효과가 있다
② 릿지는 L1 규제를 사용한다
③ 라쏘는 계수를 0으로 만들지 못한다
④ 둘 다 변수 선택 효과가 동일하다

| 내 답 | 정답 |
|---|---|
| ② 릿지는 L1 규제를 사용한다 | **① 라쏘는 L1 규제로 계수를 0으로 만들어 변수 선택 효과가 있다** |

**해설(PART B)** — L1(라쏘)은 계수를 정확히 0으로 만들어 변수 선택 효과, L2(릿지)는 축소만.

**개념 정리 — L1 vs L2 규제**

| 규제 | 기법 | 계수를 0으로 만드나 | 효과 |
|---|---|---|---|
| **L1** | **라쏘(LASSO)** | **가능** (정확히 0) | **변수 선택 O** |
| **L2** | **릿지(Ridge)** | 불가능 (0에 가깝게 축소만) | 변수 선택 X, 축소만 |

**암기팁** — **"라쏘(LASSO)는 밧줄처럼 딱 끊어낸다(0으로), 릿지(Ridge)는 능선처럼 완만하게 깎기만 한다"**
> 숫자로 기억: **L1 = 1개씩 정확히 자름(0), L2 = 제곱이라 완만하게 줄어듦(0 근처)**

**실수 포인트 ⚠️** — **"릿지는 L1을 사용한다"는 보기는 항상 오답입니다.** 릿지는 **L2**, 라쏘가 **L1**입니다. 이름과 숫자를 짝지어 못 박으세요: **라L1쏘, 릿L2지**처럼 라쏘와 1을, 릿지와 2를 나란히 외우면 순서가 헷갈리지 않습니다.

---

#### 1-4. 차원 축소 기법 ⭐구간 전체 취약⭐

> ⚠️ **34·38·39·40번, 이 소단원의 4문항이 전부 오답입니다.** 차원 축소 기법 5개(PCA·LDA·SVD·MDS·ICA)가 서로 정리되지 않은 상태로 보입니다. 아래 표 하나로 5개를 한 번에 정리하는 게 가장 빠릅니다.

### ❌ 34번 — PCA 주성분 개수 결정 기준이 아닌 것

**Q.** PCA에서 주성분의 개수를 결정하는 기준으로 적절하지 않은 것은?
① 스크리 플롯의 팔꿈치(elbow) 지점
② 누적 기여율(누적 분산 설명 비율) 기준
③ 고유값 1 이상(Kaiser 기준)
④ 종속변수와의 상관계수가 가장 높은 순서

| 내 답 | 정답 |
|---|---|
| ③ 고유값 1 이상 | **④ 종속변수와의 상관계수가 가장 높은 순서** |

**해설(PART B)** — PCA는 비지도 기법이라 종속변수를 고려하지 않는다.

**개념 정리 — 주성분 개수 결정 기준 3가지 (전부 실제 기준)**

| 기준 | 판단 방법 |
|---|---|
| **스크리 플롯 elbow** | 고유값을 그래프로 그려 **꺾이는 지점** 앞까지 |
| **누적 기여율** | 누적 분산 설명 비율이 **일정 수준(예: 80~90%)** 넘을 때까지 |
| **Kaiser 기준** | 고유값이 **1 이상**인 주성분만 채택 |

**실수 포인트 ⚠️** — ③ Kaiser 기준(고유값 1 이상)은 **실제로 존재하는 정식 기준**이라 정답이 될 수 없습니다. 함정은 ④입니다: **PCA는 비지도 학습**이라 애초에 **종속변수라는 개념 자체가 없습니다.** "종속변수"라는 단어가 PCA 관련 보기에 등장하면 거의 항상 오답 신호입니다.

---

### ❌ 38번 — 클래스 간 분산 최대화 지도학습 기법

**Q.** 클래스 간 분산은 최대화하고 클래스 내 분산은 최소화하는 축을 찾는 지도학습 기반 차원 축소 기법은?
① PCA ② LDA(선형판별분석) ③ SVD ④ t-SNE

| 내 답 | 정답 |
|---|---|
| ③ SVD | **② LDA** |

**해설(PART B)** — LDA는 클래스 레이블을 사용하는 지도학습 기반 차원 축소.

---

### ❌ 39번 — 거리 정보로 저차원에 배치

**Q.** 개체 간 거리(유사성) 정보를 이용해 저차원 공간에 개체를 배치하는 기법은?
① 군집분석 ② 주성분분석 ③ 판별분석 ④ 다차원척도법(MDS)

| 내 답 | 정답 |
|---|---|
| ② 주성분분석 | **④ 다차원척도법(MDS)** |

**해설(PART B)** — MDS = 거리(유사성) 행렬을 저차원 공간에 시각적으로 배치.

---

### ❌ 40번 — 통계적으로 독립인 성분으로 분리

**Q.** 서로 통계적으로 독립인 성분으로 신호를 분리하는 차원 축소 기법은?
① PCA ② ICA(독립성분분석) ③ LDA ④ MDS

| 내 답 | 정답 |
|---|---|
| ④ MDS | **② ICA** |

**해설(PART B)** — ICA = 통계적으로 독립인 신호 성분으로 분리(칵테일 파티 문제).

**개념 정리 — 차원 축소 기법 5개 총정리표 (34·38·39·40번 전부 해결)**

| 기법 | 지도/비지도 | 핵심 기준 | 한 줄 키워드 |
|---|---|---|---|
| **PCA** | 비지도 | **분산 최대화** | 데이터를 가장 잘 "설명"하는 축 |
| **LDA** | **지도** | **클래스 간 분산 최대화 + 클래스 내 분산 최소화** | 클래스를 가장 잘 "구분"하는 축 |
| **요인분석(FA)** | 비지도 | **잠재 요인** 규명 | 관측변수 뒤의 "숨은 원인" 찾기 |
| **SVD** | 비지도 | 행렬을 **3개로 분해** | 추천 시스템 |
| **MDS** | 비지도 | **거리(유사성)** 보존 | 가까운 건 가깝게, 먼 건 멀게 배치 |
| **ICA** | 비지도 | **통계적 독립** 성분 분리 | 칵테일 파티 문제(섞인 소리 분리) |

**암기팁 (각 기법의 "무엇을 보존/기준으로 삼는가"가 이름의 정체입니다)**
> **P**CA → **분**산(Pca와 분산 둘 다 P 계열로 연상) / **L**DA → 클래스 **레**이블(Label, 지도학습이라 정답표 필요)
> **M**DS → **거**리(Metric/Distance Scaling — 이름 자체에 Distance) / **I**CA → **독**립(Independent — 이름 자체에 Independent)
> **S**VD → 행렬 **분해**(Singular Value **Decomposition** — 이름 자체에 Decomposition)
>
> **MDS와 ICA는 이름 안에 정답 키워드(Distance, Independent)가 그대로 들어 있으므로, 영문 풀네임을 한 번씩 소리 내어 읽어보면 헷갈리지 않습니다.**

**실수 포인트 ⚠️ 이번 구간 최대 함정**
- **PCA·LDA는 익숙해서 자꾸 정답 후보로 먼저 떠오릅니다.** 39번(MDS)에 PCA를, 40번(ICA)에 MDS를 고르신 것도 **가장 익숙한 이름부터 소거해 나가다 생긴 오류**로 보입니다. 문제 속 키워드(거리→MDS, 독립→ICA)를 먼저 확정한 뒤, 그 키워드와 맞는 이름만 고르는 순서로 접근하세요.
- **"지도 vs 비지도"는 LDA만 예외로 지도**입니다. 나머지 4개(PCA·FA·SVD·MDS·ICA — 5개 중 LDA 제외 전부)는 비지도입니다. **"클래스"·"레이블"·"판별"이라는 단어가 보이면 LDA**, 그 외는 전부 비지도 계열로 좁혀집니다.

---

#### 1-5. 파생변수 · 데이터 변환

### ❌ 43번 — 양의 왜도를 완화하는 변환

**Q.** 오른쪽으로 심하게 치우친(양의 왜도) 데이터를 정규분포에 가깝게 만들기 위해 주로 사용하는 변환은?
① 로그 변환 ② 지수 변환 ③ 제곱 변환 ④ 역수 변환의 역

| 내 답 | 정답 |
|---|---|
| ③ 제곱 변환 | **① 로그 변환** |

**해설(PART B)** — 로그 변환은 큰 값을 강하게 압축해 양의 왜도를 완화한다.

**개념 정리** — 로그 변환은 **값이 클수록 더 많이 줄여주는** 함수(log)의 성질을 이용해, 오른쪽으로 길게 뻗은 꼬리(큰 값들)를 강하게 압축합니다. 그 결과 분포가 좌우 대칭에 가까워집니다.

**암기팁** — **"오른쪽 꼬리가 길면(양의 왜도) → 로그로 눌러서 짧게 만든다"**

**실수 포인트 ⚠️** — ③ 제곱 변환은 **반대로 큰 값을 더 크게 벌려서** 오른쪽 꼬리를 오히려 **더 길게** 만듭니다(왼쪽으로 치우친 음의 왜도를 완화할 때 쓰는 방향입니다). **"압축(로그)"과 "확대(제곱)"가 정반대 효과**라는 점을 기억하세요: 양의 왜도(오른쪽 꼬리) → **로그로 압축**, 음의 왜도(왼쪽 꼬리) → **제곱으로 확대**.

---

### ⚠️ 44번 — Box-Cox 변환 (헷갈림)

**Q.** 데이터를 정규분포에 가깝게 만들기 위해 최적의 λ(람다)를 찾아 멱변환을 수행하는 방법은?
① Box-Cox 변환 ② Min-Max 정규화 ③ 원-핫 인코딩 ④ 표준화

→ **정답 ① Box-Cox 변환**

**해설(PART B)** — Box-Cox 변환은 최적 λ를 찾아 정규성에 가깝게 만드는 멱변환.

**개념 정리** — **Box-Cox 변환**은 로그 변환을 일반화한 방법으로, **데이터에 가장 잘 맞는 λ(람다) 값을 통계적으로 찾아** 멱변환(거듭제곱 변환)을 수행합니다. λ=0일 때가 로그 변환과 같습니다.

**못 박을 키워드** — **"최적의 λ를 찾는다" = Box-Cox 변환**. Min-Max·표준화는 λ를 찾는 개념 자체가 없고 그냥 정해진 공식(범위 압축, 평균·표준편차 조정)을 적용할 뿐입니다.

**실수 포인트** — 43번(로그 변환)과 세트로 기억하면 좋습니다: **로그 변환은 "고정된 한 가지 방법"**, **Box-Cox는 "데이터에 맞춰 λ를 최적화하는 로그 변환의 확장판"**입니다.

---

### ❌ 45번 — 연속형을 구간으로 나눠 범주화

**Q.** 연속형 변수를 몇 개의 구간으로 나누어 범주형으로 만드는 기법은?
① 정규화 ② 스무딩 ③ 비닝(Binning) ④ 차원 축소

| 내 답 | 정답 |
|---|---|
| ② 스무딩 | **③ 비닝(Binning)** |

**해설(PART B)** — 비닝 = 연속형을 구간으로 나눠 범주화.

**개념 정리 — 헷갈리기 쉬운 전처리 용어 구분**

| 용어 | 뜻 |
|---|---|
| **비닝(Binning)** | 연속형 값을 **구간(bin)으로 나눠** 범주형으로 변환 |
| **스무딩(Smoothing)** | 값을 **평활화**(구간 평균·중앙값 등으로 대체해 잡음 완화) — 비닝의 활용 목적 중 하나 |
| 정규화 | 값의 **범위를 재조정**(스케일링) |

**암기팁** — **"비닝 = 통(bin)에 나눠 담기"** — bin이라는 단어 자체가 '통·상자'라는 뜻입니다. 연속된 값을 여러 개의 상자(구간)에 나눠 담는 그림을 떠올리면 됩니다.

**실수 포인트 ⚠️** — 스무딩(평활화)은 **비닝 기법을 활용해서 잡음을 줄이는 후속 작업 중 하나**일 뿐, "구간으로 나누는" 정의 자체를 가리키는 말이 아닙니다. **문제가 "구간으로 나눈다"는 정의를 직접 물으면 비닝**, **"잡음을 줄인다"는 목적을 물으면 스무딩** 쪽에 가깝습니다. 이 둘은 서로 관련은 있지만 **정의(비닝) vs 목적/효과(스무딩)**로 층위가 다릅니다.

---

---

#### 1-6. 스케일링 · 클래스 불균형 처리

### ❌ 48번 — 중앙값·IQR 기반 스케일링

**Q.** 이상값의 영향을 줄이기 위해 중앙값과 IQR을 사용하는 스케일링 기법은?
① 로버스트 스케일링 ② 표준화 ③ 최소-최대 정규화 ④ 로그 변환

| 내 답 | 정답 |
|---|---|
| ② 표준화 | **① 로버스트 스케일링** |

**해설(PART B)** — 로버스트 스케일링 = (x−중앙값)/IQR, 이상값에 강건.

**개념 정리 — 스케일링 3형제 비교**

| 기법 | 계산 | 기준값 | 이상값에 강한가 |
|---|---|---|---|
| **Min-Max 정규화** | (x−min)/(max−min) | 최솟값·최댓값 | **약함** (이상값이 범위를 왜곡) |
| **표준화(Z-score)** | (x−평균)/표준편차 | **평균·표준편차** | 약함 (평균·표준편차가 이상값에 흔들림) |
| **로버스트 스케일링** | **(x−중앙값)/IQR** | **중앙값·IQR** | **강함** |

**암기팁** — **"로버스트(Robust) = 튼튼하다"** — 이상값이 있어도 잘 안 흔들리는 **중앙값·IQR**을 씁니다. 평균·표준편차는 극단값 하나에도 크게 출렁이지만, 중앙값·IQR은 순위 기반이라 잘 흔들리지 않습니다.

**실수 포인트 ⚠️** — 표준화(Z-score)를 고르신 건 "표준적인 방법이니까 이상값에도 안전할 것"이라는 직관 때문일 수 있습니다. 하지만 **표준화는 평균·표준편차를 쓰는데, 이 둘은 이상값 하나에도 크게 움직이는 통계량**입니다. 이상값 이야기가 나오면 **"중앙값·IQR" = 로버스트**로 바로 연결하세요 (이상값 탐지 때 IQR을 쓰는 것과 같은 원리입니다).

---

### ❌ 54번 — 최근접 이웃 보간 합성 기법

**Q.** 소수 클래스 관측치와 그 최근접 이웃 사이를 보간하여 새로운 합성 데이터를 생성하는 기법은?
① SMOTE ② 랜덤 언더샘플링 ③ 토멕 링크 ④ ENN

| 내 답 | 정답 |
|---|---|
| ④ ENN | **① SMOTE** |

**해설(PART B)** — SMOTE = 소수 클래스와 최근접 이웃 사이를 보간해 합성 데이터 생성.

**개념 정리 — 클래스 불균형 처리 기법**

| 기법 | 방향 | 방법 | 단점 |
|---|---|---|---|
| **언더샘플링** | 다수 클래스 **줄이기** | 무작위 제거 | **정보 손실** |
| **오버샘플링(단순 복제)** | 소수 클래스 **늘리기** | 기존 데이터 복제 | **과적합** |
| **SMOTE** | 소수 클래스 **늘리기** | 최근접 이웃 사이를 **보간**해 새 데이터 **합성** | 경계가 모호해질 수 있음 |
| 토멕 링크 / ENN | 다수 클래스 **정제** | 경계에 가까운 다수 클래스 샘플 제거 | 언더샘플링 계열의 정제 기법 |

**암기팁** — **"SMOTE는 복제가 아니라 합성(Synthetic)"** — 이름 자체가 **S**ynthetic **M**inority **O**ver-sampling **TE**chnique입니다. 단순 복제(오버샘플링)와 달리, **기존 점과 이웃 점 사이의 새로운 위치에 데이터를 만들어냅니다.**

**실수 포인트 ⚠️** — ④ ENN(Edited Nearest Neighbor)은 **다수 클래스를 "정제"하는 언더샘플링 계열 기법**이라 "새로운 데이터를 생성"하는 것과는 방향이 정반대입니다. **"생성·합성·보간"이라는 단어가 있으면 SMOTE**, **"제거·정제"라는 단어가 있으면 언더샘플링 계열(토멕 링크·ENN)**로 방향을 먼저 가르세요.

---

## PART 2. 데이터 탐색 (EDA · 상관 · 시각화)

#### 2-1. EDA 개념 · 창시자 · 4대 주제 ⭐연쇄 오답⭐

> ⚠️ **56·57·58번이 연달아 틀렸습니다.** EDA를 만든 사람(56), EDA의 4대 주제가 무엇인지(57), 그중 하나인 '저항성'의 정의(58)까지 — **EDA의 기본 뼈대 자체가 아직 정리되지 않은 상태**입니다. 아래 표로 한 번에 잡으면 이 구간 전체가 풀립니다.

### ❌ 56번 — EDA를 처음 제시한 통계학자

**Q.** 탐색적 데이터 분석(EDA)의 개념을 처음 제시한 통계학자는?
① 로널드 피셔 ② 윌리엄 고셋 ③ 칼 피어슨 ④ 존 튜키

| 내 답 | 정답 |
|---|---|
| ③ 칼 피어슨 | **④ 존 튜키** |

**해설(PART B)** — EDA는 존 튜키(John Tukey)가 제시.

**개념 정리 — 통계학자 4인 구분**

| 인물 | 대표 업적 |
|---|---|
| **존 튜키** | **EDA(탐색적 데이터 분석) 창시** |
| 칼 피어슨 | **피어슨 상관계수**, 카이제곱 검정 |
| 로널드 피셔 | 분산분석(ANOVA), 최대우도추정(MLE) |
| 윌리엄 고셋 | **t분포**("Student") |

**암기팁** — **"튜키(Tukey)는 EDA를 튜닝(Tuning)했다"** — 이름의 어감으로 연결하면 기억하기 쉽습니다. 1과목 오답노트에서도 "EDA=존 튜키"가 등장했던 것과 같은 인물입니다.

**실수 포인트 ⚠️** — 칼 피어슨은 **상관계수·카이제곱**으로 유명해서 "통계 분석"이라는 단어와 함께 나오면 먼저 떠오르기 쉽습니다. 하지만 **"탐색적(Exploratory)"이라는 단어가 있으면 항상 존 튜키**입니다. 네 인물 모두 서로 다른 개념 하나씩과 짝지어 두면 헷갈리지 않습니다.

---

### ❌ 57번 — EDA 4가지 주제가 아닌 것

**Q.** EDA의 4가지 주제에 해당하지 않는 것은?
① 저항성 ② 잔차 해석 ③ 자료 재표현 ④ 인과성

| 내 답 | 정답 |
|---|---|
| ① 저항성 | **④ 인과성** |

**해설(PART B)** — EDA 4주제 = 저항성·잔차 해석·자료 재표현·현시성. 인과성은 아니다.

**개념 정리 — EDA 4대 주제**

| 주제 | 뜻 |
|---|---|
| **저항성(Resistance)** | 소수의 **이상값에 크게 영향받지 않는** 성질 |
| **잔차 해석(Residual)** | 모형과 실제값의 **차이(잔차)**를 분석 |
| **자료 재표현(Re-expression)** | 변환(로그 등)으로 **해석하기 쉬운 형태**로 바꿈 |
| **현시성(Graphical Representation)** | **그래프로 시각화**하여 구조를 드러냄 |

**암기팁** — **"저·잔·재·현"** 4글자로 외우세요.

**실수 포인트 ⚠️ "아닌 것" 문제 습관** — ①(저항성)을 고르신 건 **1과목 오답노트에서 정리했던 "아닌 것 문제 = 익숙한 보기를 지운다" 오류와 같은 패턴**입니다. 저항성은 **4대 주제의 정식 멤버**입니다. 반면 **④ 인과성(Causality)은 EDA가 아니라 "인과 추론"이라는 전혀 다른 통계 분야의 용어**라 애초에 EDA 4주제 목록에 있을 수 없습니다. **보기 중 "이 분야 용어가 아닌 것"을 찾는 방향으로 접근**하면 인과성이 바로 눈에 띕니다.

---

### ❌ 58번 — '저항성'의 정의

**Q.** EDA의 '저항성(Resistance)'에 대한 설명으로 옳은 것은?
① 소수의 이상값에 크게 영향받지 않는 성질
② 그래프로 표현하는 성질
③ 자료를 변환하여 해석하는 성질
④ 잔차를 분석하는 성질

| 내 답 | 정답 |
|---|---|
| ④ 잔차를 분석하는 성질 | **① 소수의 이상값에 크게 영향받지 않는 성질** |

**해설(PART B)** — 저항성 = 이상값에 둔감한 성질(중앙값·IQR 등).

**실수 포인트 ⚠️ 57번과 이어지는 실수** — 이 문제의 보기 ②③④는 각각 **현시성·자료재표현·잔차해석의 정의를 그대로 가져온 것**입니다. 즉 **"저항성이 무엇인지 몰라서" 틀렸다기보다, "EDA 4대 주제 중 나머지 3개의 정의와 헷갈려서"** 틀린 것에 가깝습니다. 57번에서 4대 주제 목록 자체가 흔들렸기 때문에 58번도 함께 흔들린 것입니다 → **57번의 "저·잔·재·현" 표만 확실히 잡으면 58번은 저절로 따라옵니다.**

**함께 저장할 것** — **"저항성 = 48번(로버스트 스케일링)과 같은 개념"**입니다. 로버스트 스케일링이 중앙값·IQR로 이상값에 강한 것처럼, **저항성도 "이상값에 흔들리지 않는 성질"**이라는 같은 아이디어입니다. 두 문항을 한 세트로 기억하면 서로를 강화합니다.

---

### ⚠️ 59번 — '현시성'의 의미 (헷갈림)

**Q.** EDA의 '현시성(Graphical Representation)'이 의미하는 것은?
① 데이터를 그래프로 시각화하여 구조를 파악하는 것
② 데이터를 삭제하는 것
③ 모형을 수식으로 표현하는 것
④ 가설을 검정하는 것

→ **정답 ① 데이터를 그래프로 시각화하여 구조를 파악하는 것**

**해설(PART B)** — 현시성 = 그래프를 통해 구조를 드러내는 것.

**못 박을 키워드** — **"현시성"의 '시(示)' = 보여주다** → **그래프로 "보여주는" 성질**. 한자 뜻 그대로입니다.

**실수 포인트** — 영문 병기(Graphical Representation)가 문제에 그대로 있어서 사실상 서비스 문제에 가깝습니다. ②③④는 EDA의 다른 어떤 주제와도 관련 없는 완전히 무관한 활동(삭제·수식화·가설검정)이라 저항성·잔차해석·자료재표현처럼 서로 헷갈릴 여지도 적습니다. **57~59번 구간에서 가장 먼저 확실하게 못 박아 둘 문항입니다.**

---

#### 2-2. 대푯값의 종류와 분포의 모양 ⭐교차 오답⭐

> ⚠️ **63번과 64번이 서로 바뀌었습니다.** 63(비율·성장률→기하평균이 정답)에 조화평균을, 64(속도→조화평균이 정답)에 기하평균을 쓰셨습니다. **두 평균의 "이름"과 "쓰임" 자체는 알고 계시는데, 어느 쪽이 어느 쪽인지 짝이 뒤집힌 상태**입니다.

### ❌ 63번 — 비율·성장률의 평균

**Q.** 성장률·수익률처럼 비율의 평균을 구할 때 적절한 평균은?
① 산술평균 ② 기하평균 ③ 조화평균 ④ 절사평균

| 내 답 | 정답 |
|---|---|
| ③ 조화평균 | **② 기하평균** |

**해설(PART B)** — 비율·성장률의 평균은 기하평균.

### ❌ 64번 — 단위당 값(속도)의 평균

**Q.** 속도처럼 단위당 값의 평균을 구할 때 적절한 평균은?
① 산술평균 ② 기하평균 ③ 조화평균 ④ 가중평균

| 내 답 | 정답 |
|---|---|
| ② 기하평균 | **③ 조화평균** |

**해설(PART B)** — 속도 등 단위당 값의 평균은 조화평균.

**개념 정리 — 평균 3형제**

| 평균 | 쓰는 상황 | 예시 |
|---|---|---|
| **산술평균** | 일반적인 값들의 평균 | 시험 점수 평균 |
| **기하평균** | **비율·배율(곱셈적으로 누적되는 값)**의 평균 | **성장률·수익률·물가상승률** |
| **조화평균** | **단위당 값(역수 관계)**의 평균 | **속도, 시간당 생산량** |

**암기팁** — **"기하평균은 곱해지는 것(%), 조화평균은 나눠지는 것(단위/시간)"**
> 성장률은 매년 **곱해져서** 누적됩니다(기하급수적 증가 = 기하평균). 속도는 거리를 시간으로 **나눈** 값이라 역수 관계가 핵심입니다(조화평균).
> 못 박는 한 글자: **"기하학적으로 커진다 → 기하평균", "조화롭게 역수를 맞춘다 → 조화평균"**

**실수 포인트 ⚠️** — 두 평균 모두 "산술평균이 아닌 특수한 평균"이라는 것까지는 알고 계셨지만, **이름과 상황을 반대로 저장**하고 계셨습니다. **"%로 표현되는 값(비율)=기하", "단위/단위 로 표현되는 값(속도·밀도)=조화"** 라는 기준으로 한쪽만 확실히 못 박으면 나머지는 자동으로 정리됩니다.

---

### ❌ 65번 — 양의 왜도에서 대푯값의 크기 순서

**Q.** 오른쪽으로 긴 꼬리를 갖는(양의 왜도) 분포에서 대표값의 크기 관계로 옳은 것은?
① 평균 < 중앙값 < 최빈값
② 최빈값 < 중앙값 < 평균
③ 중앙값 < 평균 < 최빈값
④ 평균 = 중앙값 = 최빈값

| 내 답 | 정답 |
|---|---|
| ① 평균 < 중앙값 < 최빈값 | **② 최빈값 < 중앙값 < 평균** |

**해설(PART B)** — 양의 왜도(오른쪽 꼬리)에서는 최빈값 < 중앙값 < 평균.

**개념 정리** — 오른쪽 꼬리가 길면(양의 왜도), **평균이 그 긴 꼬리 쪽으로 끌려가서 가장 커집니다.** 최빈값(가장 흔한 값, 봉우리)은 꼬리와 무관하게 그대로 있어 가장 작습니다.

**암기팁** — **"꼬리가 평균을 잡아당긴다"** — 오른쪽 꼬리 → 평균이 오른쪽(큰 쪽)으로 끌려감 → **최빈 < 중앙 < 평균**. 왼쪽 꼬리(음의 왜도)면 정반대로 **평균 < 중앙 < 최빈**입니다.

**실수 포인트 ⚠️** — ①을 고르신 건 **순서를 정반대로 기억**하신 경우입니다. "오른쪽 꼬리 = 큰 값들이 드물게 있음 = 평균이 커짐"이라는 인과관계를 먼저 떠올리면 방향을 헷갈리지 않습니다. **PART C 최다 함정에도 있는 최빈출 포인트**이니 꼭 확실히 잡으세요.

---

#### 2-3. 산포도 · 변동계수 ⭐구간 취약⭐

> ⚠️ **67·68·69번, 이 소단원 3문항이 전부 오답이거나 헷갈렸습니다.** 산포도 통계량의 종류(67)와 그중 변동계수(CV)가 무엇이고(68) 어떻게 계산하는지(69)가 통째로 흔들리는 상태입니다.

### ⚠️ 67번 — 산포도 통계량이 아닌 것 (헷갈림)

**Q.** 자료의 산포도를 나타내는 통계량이 아닌 것은?
① 범위 ② 사분위범위(IQR) ③ 변동계수 ④ 최빈값

→ **정답 ④ 최빈값**

**해설(PART B)** — 최빈값은 중심 경향 측도.

**개념 정리** — **중심 경향(대푯값)**: 평균·중앙값·최빈값 / **산포도(퍼짐 정도)**: 범위·IQR·분산·표준편차·변동계수. **최빈값은 "어디에 몰려있나"를 보는 대푯값**이지, "얼마나 퍼져있나"를 보는 산포도가 아닙니다.

---

### ❌ 68번 — 단위가 다른 집단의 상대적 산포 비교

**Q.** 단위가 서로 다른 두 집단의 산포를 비교할 때 사용하는 상대적 산포 측도는?
① 분산 ② 표준편차 ③ 변동계수(CV) ④ 범위

| 내 답 | 정답 |
|---|---|
| ① 분산 | **③ 변동계수(CV)** |

**해설(PART B)** — 변동계수는 단위가 다른 집단의 상대적 산포 비교에 사용.

---

### ⚠️ 69번 — 변동계수(CV)의 계산식 (헷갈림)

**Q.** 변동계수(CV)의 계산식으로 옳은 것은?
① 표준편차 ÷ 평균 ② 평균 ÷ 표준편차 ③ 분산 ÷ 평균 ④ 표준편차 ÷ 중앙값

→ **정답 ① 표준편차 ÷ 평균**

**해설(PART B)** — CV = 표준편차 ÷ 평균.

**개념 정리 — 왜 변동계수가 필요한가 (68·69번 동시 해결)**

> 분산·표준편차는 **원래 단위 그대로**라서, "몸무게(kg)의 산포"와 "키(cm)의 산포"를 직접 비교할 수 없습니다. **변동계수 = 표준편차 ÷ 평균**으로 **단위를 없애면(무차원화)**, 서로 다른 단위·척도의 산포를 공정하게 비교할 수 있습니다.

**암기팁** — **"CV = Coefficient of Variation = 표준편차를 평균으로 나눠 '상대적 크기'로 바꾼 것"** — 분자가 산포(표준편차), 분모가 기준(평균)입니다. **"산포 ÷ 기준"**이라는 순서만 기억하면 됩니다(거꾸로 하면 뜻이 사라집니다).

**실수 포인트 ⚠️** — 68번에서 분산·표준편차를 고르신 건 "산포도 통계량이니까"라는 방향은 맞았지만, **"단위가 다른 집단을 비교"한다는 조건을 놓치신 것**입니다. 분산·표준편차는 원래 단위를 그대로 갖고 있어 **서로 다른 단위끼리는 비교 불가**하다는 점이 핵심 함정입니다. "단위가 다르다"·"상대적"이라는 단어가 보이면 바로 변동계수(CV)로 연결하세요.

---

#### 2-4. 첨도

### ❌ 72번 — 정규분포의 첨도값

**Q.** 정규분포의 첨도(Kurtosis) 값은?
① 0 ② 1 ③ 3 ④ 무한대

| 내 답 | 정답 |
|---|---|
| ② 1 | **③ 3** |

**해설(PART B)** — 정규분포 첨도 = 3 (초과첨도 기준으로는 0).

**개념 정리 — 첨도의 두 가지 표기법에 주의**

| 표기 | 정규분포 값 | 특징 |
|---|---|---|
| **첨도(Kurtosis)** | **3** | 원래 정의 그대로 |
| **초과첨도(Excess Kurtosis)** | **0** | 첨도에서 3을 뺀 값(소프트웨어 기본 출력) |

**암기팁** — **"첨도는 3, 초과는 0"** — 문제에 "초과(Excess)"라는 단어가 없으면 **3**, 있으면 **0**입니다. 둘을 헷갈리면 정반대 답을 고르게 되므로, **문제에 '초과'라는 단어가 있는지부터 확인**하는 습관을 들이세요.

**실수 포인트 ⚠️** — ②(1)을 고르신 건 정규분포의 **분산(표준정규분포에서 분산=1)**과 헷갈리신 것으로 보입니다. **분산=1은 표준정규분포 이야기, 첨도=3은 정규분포 전체(표준화 여부와 무관)의 이야기**로 서로 다른 통계량입니다.

---

#### 2-5. 그래프 해석 — 히스토그램 · 박스플롯 · Q-Q 플롯

### ❌ 76번 — 히스토그램과 막대그래프의 차이

**Q.** 히스토그램과 막대그래프의 차이로 옳은 것은?
① 히스토그램은 범주형, 막대그래프는 연속형 데이터에 사용한다
② 히스토그램은 연속형 데이터의 구간별 도수를 나타내며 막대가 붙어 있다
③ 둘은 완전히 동일하다
④ 막대그래프는 면적으로 도수를 표현한다

| 내 답 | 정답 |
|---|---|
| ① 히스토그램은 범주형, 막대그래프는 연속형 | **② 히스토그램은 연속형, 막대가 붙어 있다** |

**해설(PART B)** — 히스토그램은 연속형 구간 도수를 막대가 붙은 형태로 표현.

**개념 정리 — 완전히 반대로 기억하고 계셨던 지점**

| 그래프 | 데이터 유형 | 막대 사이 간격 |
|---|---|---|
| **히스토그램** | **연속형**(구간으로 나눔) | **붙어 있음** (구간이 이어지므로) |
| **막대그래프** | **범주형**(항목별 비교) | **떨어져 있음** (항목이 독립적이므로) |

**암기팁** — **"연속된 숫자는 막대도 연속(붙음), 서로 다른 범주는 막대도 서로 다름(떨어짐)"** — 그래프의 생김새가 데이터 성질을 그대로 반영한다고 생각하면 방향이 저절로 잡힙니다.

**실수 포인트 ⚠️** — ①번은 **정의를 정확히 반대로 뒤집은 보기**입니다. 이런 "완전 반대" 함정은 짐작으로 풀면 50%로 틀리기 쉬우므로, **"막대가 붙어 있으면 히스토그램(연속형)"**이라는 시각적 특징 하나만 확실히 못 박아 두세요.

---

### ❌ 77번 — 상자수염그림의 상자 경계

**Q.** 상자수염그림(Box Plot)에서 상자의 위·아래 경계가 의미하는 것은?
① 최댓값과 최솟값 ② 제3사분위수와 제1사분위수 ③ 평균 ± 표준편차 ④ 중앙값과 평균

| 내 답 | 정답 |
|---|---|
| ① 최댓값과 최솟값 | **② 제3사분위수와 제1사분위수** |

**해설(PART B)** — 상자의 위=Q3, 아래=Q1, 내부 선=중앙값.

**개념 정리 — 박스플롯의 5개 구성 요소**

| 부위 | 의미 |
|---|---|
| 상자 위쪽 경계 | **Q3(제3사분위수)** |
| 상자 내부 선 | **중앙값(Q2)** |
| 상자 아래쪽 경계 | **Q1(제1사분위수)** |
| 수염(whisker) 끝 | Q1−1.5×IQR ~ Q3+1.5×IQR 범위 내 최댓값·최솟값 |
| 수염 밖 점 | **이상값** |

**암기팁** — **"상자 = 가운데 50%(Q1~Q3), 수염 끝 = 그 바깥 정상 범위, 점 = 이상값"** — **최댓값·최솟값은 상자가 아니라 '수염 끝'에 가깝게 걸리는 것**이고, 상자 자체의 경계는 항상 Q1·Q3입니다.

**실수 포인트** — ①(최댓값·최솟값)은 **수염(whisker)의 개념과 상자(box)의 개념을 혼동**한 것입니다. 78번(박스플롯으로 알 수 없는 것=정확한 평균값)과 함께, **"박스플롯 = 사분위수 기반 그림이지 평균·최댓값을 직접 보여주는 그림이 아니다"**라는 원칙으로 접근하면 정리됩니다.

---

### ⚠️ 80번 — 이론 분포 적합도를 시각 확인하는 그래프 (헷갈림)

**Q.** 데이터가 특정 이론적 분포(예: 정규분포)를 따르는지 시각적으로 확인하는 그래프는?
① 히트맵 ② 파레토 차트 ③ 트리맵 ④ Q-Q 플롯

→ **정답 ④ Q-Q 플롯**

**해설(PART B)** — Q-Q 플롯으로 이론 분포와의 적합 여부를 시각 확인.

**못 박을 키워드** — **Q-Q = Quantile-Quantile(분위수-분위수)** — 실제 데이터의 분위수와 이론 분포의 분위수를 점으로 찍어, **점들이 대각선(y=x)에 가까울수록 그 분포를 따른다**고 판단합니다. PART C의 **"정규성 검정: 샤피로-윌크, 콜모고로프-스미르노프, Q-Q 플롯"**과 함께 묶어 기억하세요.

---

#### 2-6. 공분산 · 상관계수 ⭐구간 취약⭐

> ⚠️ **81·84·85번, 상관분석 관련 문항 3개가 전부 오답입니다.** 공분산의 한계(81), 순위 상관계수 종류(84), 상관계수 0의 의미(85)가 서로 다른 각도에서 "상관"이라는 하나의 주제를 흔들고 있습니다.

### ❌ 81번 — 공분산의 한계

**Q.** 공분산(Covariance)의 한계로 옳은 것은?
① 방향을 알 수 없다
② 변수의 측정 단위에 따라 값이 달라져 관계의 강도 비교가 어렵다
③ 항상 0과 1 사이 값이다
④ 음수가 될 수 없다

| 내 답 | 정답 |
|---|---|
| ① 방향을 알 수 없다 | **② 단위에 따라 값이 달라져 비교 어려움** |

**해설(PART B)** — 공분산은 단위에 의존해 크기 비교가 불가 → 상관계수로 표준화.

**개념 정리** — 공분산은 **부호(방향, +/−)는 알 수 있지만, 크기가 변수의 단위에 그대로 영향받아 "관계가 얼마나 강한지" 비교할 수 없습니다.** 이 한계를 해결하려고 공분산을 표준편차로 나눠 **단위 없는 상관계수**로 만든 것입니다.

**암기팁** — **"공분산은 방향은 알려주지만 세기는 못 알려준다"** — ①(방향을 모른다)은 사실과 반대입니다. 공분산의 **부호(+/−)가 곧 방향**이고, 문제가 되는 건 **크기(세기) 비교가 안 된다는 점**입니다.

**실수 포인트 ⚠️** — "한계·단점"을 묻는 문제에서 **"~을 알 수 없다"는 보기가 나오면 반사적으로 고르기 쉽지만, 공분산은 방향(부호)만큼은 정확히 알려줍니다.** 문제가 되는 지점은 "단위 의존성"이라는 걸 정확히 짚어야 합니다.

---

### ❌ 84번 — 일치쌍·불일치쌍 기반 순위 상관계수

**Q.** 순위 자료에서 일치쌍과 불일치쌍의 개수를 이용해 상관을 측정하는 계수는?
① 켄달의 타우 ② 피어슨 상관계수 ③ 크래머의 V ④ 파이 계수

| 내 답 | 정답 |
|---|---|
| ② 피어슨 상관계수 | **① 켄달의 타우** |

**해설(PART B)** — 켄달의 타우 = 일치쌍·불일치쌍 기반 순위 상관.

**개념 정리 — 상관계수 4형제 (변수 유형별로 짝짓기)**

| 계수 | 변수 유형 | 계산 방식 |
|---|---|---|
| **피어슨** | 등간·비율(연속형) | 선형 관계의 세기 |
| **스피어만** | 순위(서열) | 순위 차이 기반 |
| **켄달의 타우** | 순위(서열) | **일치쌍·불일치쌍** 개수 기반 |
| 카이제곱·크래머의 V·파이 계수 | 범주형 | 교차표 기반 연관성 |

**실수 포인트 ⚠️** — 피어슨은 **연속형 데이터에 쓰는 계수**라 "순위 자료"라는 조건과 애초에 맞지 않습니다. **"순위·서열"이라는 단어가 보이면 피어슨은 자동 제외**하고 스피어만·켄달 중에서 고르세요. 이번엔 그중 **"일치쌍·불일치쌍"이라는 구체적 계산 방식이 언급된 켄달의 타우**가 정답이었습니다.

---

### ❌ 85번 — 피어슨 상관계수 0의 의미

**Q.** 피어슨 상관계수가 0이라는 것의 올바른 해석은?
① 두 변수는 완전히 독립이다
② 두 변수 사이에 선형 관계가 없다는 뜻이며, 비선형 관계는 있을 수 있다
③ 두 변수는 아무 관계가 없다
④ 인과관계가 없다는 것을 증명한다

| 내 답 | 정답 |
|---|---|
| ① 두 변수는 완전히 독립이다 | **② 선형 관계가 없다는 뜻(비선형 관계는 있을 수 있음)** |

**해설(PART B)** — r=0은 '선형' 관계가 없다는 뜻이며 독립을 의미하지 않는다.

**실수 포인트 ⚠️ 1과목·PART C 최다 함정과 동일한 규칙** — **"상관계수 0 = 선형관계 없음이지 독립이 아니다"**는 이 시험에서 가장 자주 나오는 함정 중 하나입니다(PART C 최다 함정 TOP 10의 2번 항목). **"관계가 없다"·"독립이다"라는 강한 단정형 보기는 항상 오답**이고, **"선형" 관계라는 한정어가 붙은 보기가 정답**입니다. 곡선(비선형) 관계는 상관계수로 못 잡아낸다는 점이 핵심입니다.

**함께 저장할 것** — 81번(공분산의 한계)·85번(r=0의 의미)을 묶어서: **"공분산·상관계수는 오직 '직선(선형) 관계'만 재는 도구"**라는 하나의 큰 원칙으로 저장하세요. 두 문항 모두 이 원칙에서 파생됩니다.

---

#### 2-7. 공간 시각화 · 시각화 프로세스

### ❌ 95번 — 지도 면적을 왜곡하는 공간 시각화

**Q.** 지역별 통계값의 크기에 따라 지도의 면적 자체를 왜곡하여 표현하는 공간 시각화 기법은?
① 등치지역도(코로플레스) ② 도트맵 ③ 카토그램 ④ 버블맵

| 내 답 | 정답 |
|---|---|
| ① 등치지역도 | **③ 카토그램** |

**해설(PART B)** — 카토그램 = 값의 크기에 따라 지도 면적을 왜곡.

**개념 정리 — 공간 시각화 4형제**

| 기법 | 표현 방식 |
|---|---|
| **등치지역도(코로플레스)** | 지역을 **색상 농담**으로 구분(면적은 그대로) |
| **도트맵** | 점을 찍어 **밀도**를 표현 |
| **카토그램** | 값의 크기에 따라 **지도 면적 자체를 왜곡** |
| **버블맵** | 지도 위에 값 크기만큼 **원(버블)**을 얹음 |

**암기팁** — **"등치지역도=색깔만 바뀜(면적 그대로) / 카토그램=면적 자체가 바뀜"** — 색상만 다르게 칠하는지, 땅 모양 자체를 찌그러뜨리는지로 구분하세요.

**실수 포인트 ⚠️** — 등치지역도는 **"색상 농담"**으로 크기를 표현하지 **"면적을 왜곡"**하지 않습니다. "면적 자체를 왜곡·변형한다"는 문구가 있으면 무조건 **카토그램**입니다.

---

### ❌ 100번 — 데이터 시각화 절차의 순서

**Q.** 데이터 시각화 절차의 순서로 옳은 것은?
① 구조화 → 시각화 → 시각 표현
② 시각화 → 구조화 → 시각 표현
③ 시각 표현 → 구조화 → 시각화
④ 구조화 → 시각 표현 → 시각화

| 내 답 | 정답 |
|---|---|
| ④ 구조화 → 시각 표현 → 시각화 | **① 구조화 → 시각화 → 시각 표현** |

**해설(PART B)** — 시각화 절차 = 구조화 → 시각화 → 시각 표현.

**개념 정리** — **구조화**(데이터를 목적에 맞게 정리) → **시각화**(그래프·차트로 그리기) → **시각 표현**(색상·주석 등으로 다듬어 완성) — **"뼈대를 잡고 → 그림을 그리고 → 꾸민다"**는 상식적인 순서입니다.

**실수 포인트 ⚠️** — ④는 **뒤의 두 단계(시각화·시각 표현) 순서가 서로 바뀐 것**입니다. "꾸미기(시각 표현)"는 "그리기(시각화)"가 끝난 다음에야 할 수 있는 작업이므로, **그림을 그리기도 전에 꾸밀 수는 없다**는 상식으로 순서를 점검하면 됩니다. 1과목·2과목에서 반복된 **"순서 문제는 상식적 선후관계로 점검"** 원칙이 여기서도 그대로 적용됩니다.

---

## PART 3. 통계기법 (확률분포 · 표본추출 · 추정 · 검정)

#### 3-1. 확률의 기초

### ❌ 101번 — 모든 결과의 집합

**Q.** 어떤 실험에서 일어날 수 있는 모든 결과의 집합을 무엇이라 하는가?
① 사건 ② 확률변수 ③ 표본공간 ④ 모집단

| 내 답 | 정답 |
|---|---|
| ① 사건 | **③ 표본공간** |

**해설(PART B)** — 모든 가능한 결과의 집합 = 표본공간.

**개념 정리 — 확률 기초 용어 구분**

| 용어 | 뜻 |
|---|---|
| **표본공간(S)** | 일어날 수 있는 **모든** 결과의 집합 (예: 주사위 → {1,2,3,4,5,6}) |
| **사건(Event)** | 표본공간의 **부분집합** (예: 짝수가 나옴 → {2,4,6}) |
| 확률변수 | 결과를 **숫자로 대응**시키는 함수 |

**암기팁** — **"표본공간이 큰 그릇, 사건은 그 그릇 안의 일부"** — 사건은 표본공간의 부분집합이므로, "전체"를 묻는 문제에는 항상 표본공간이 답입니다.

**실수 포인트** — ①(사건)은 **부분**을 가리키는 말이라 "모든 결과"라는 **전체**를 묻는 질문과 맞지 않습니다. "모든·전체"라는 단어가 있으면 표본공간, "특정 하나의 결과 묶음"을 가리키면 사건입니다.

---

### ⚠️ 106번 — 배타적 분할로 전체 확률을 구하는 정리 (헷갈림)

**Q.** 표본공간을 서로 배타적인 사건들로 분할하여 전체 확률을 구하는 정리는?
① 전확률 정리 ② 베이즈 정리 ③ 곱셈정리 ④ 여사건 정리

→ **정답 ① 전확률 정리**

**해설(PART B)** — 전확률 정리 = 배타적 분할 사건들로 전체 확률 계산.

**개념 정리 — 전확률 정리 vs 베이즈 정리**

| 정리 | 방향 | 하는 일 |
|---|---|---|
| **전확률 정리** | **원인들 → 전체 결과** | 여러 조건(원인)으로 나눠 **전체 확률을 합산** |
| **베이즈 정리** | **결과 → 원인**(역추론) | 결과가 관측된 후, **어느 원인이었을지** 사후확률로 갱신 |

**못 박을 키워드** — **"분할해서 더한다" = 전확률**, **"거꾸로 추론한다(사후확률 갱신)" = 베이즈**. 사실 **베이즈 정리의 분모 자체가 전확률 정리로 계산**되므로 두 정리는 세트로 쓰이는 경우가 많습니다.

---
### ❌ 107번 — 확률변수에 대한 설명 중 옳지 않은 것

**Q.** 확률변수에 대한 설명으로 옳지 않은 것은?
① 이산확률변수는 셀 수 있는 값을 갖는다
② 연속확률변수는 구간 내 임의의 실수 값을 갖는다
③ 연속확률변수에서 특정 한 점의 확률은 0이다
④ 이산확률변수는 확률밀도함수로 표현한다

| 내 답 | 정답 |
|---|---|
| ② 연속확률변수는 구간 내 임의의 실수 값을 갖는다 | **④ 이산확률변수는 확률밀도함수로 표현한다** |

**해설(PART B)** — 이산확률변수는 확률질량함수(PMF), 연속이 확률밀도함수(PDF).

**개념 정리 — 이산 vs 연속 확률변수**

| 구분 | 값의 형태 | 사용 함수 |
|---|---|---|
| **이산확률변수** | 셀 수 있는 값(정수 등) | **확률질량함수(PMF)** |
| **연속확률변수** | 구간 내 임의의 실수 | **확률밀도함수(PDF)** |

**암기팁** — **"이산은 질량(Mass, 콕 집어 셀 수 있는 덩어리), 연속은 밀도(Density, 구간에 퍼진 정도)"**

**실수 포인트 ⚠️** — ②를 오답으로 고르신 건, **②가 사실은 100% 맞는 설명이라 "옳지 않은 것"이 될 수 없다는 점**을 놓치신 경우입니다. 연속확률변수는 정의 그대로 "구간 내 임의의 실수 값"을 가집니다. 함정은 ④입니다: **"이산"인데 "확률밀도함수(PDF)"라고 한 것이 모순**입니다 — 이산은 PMF, 연속은 PDF로 이름 자체가 다릅니다.

---

#### 3-2. 확률분포 총정리 ⭐구간 전체 취약⭐

> ⚠️ **111~120번 구간에서 5문항(114·115·116·119·120)이 오답·헷갈림입니다.** 이산분포(기하·초기하)와 연속분포(균등·지수)의 정의가 서로 섞인 상태입니다. 아래 총정리표 하나로 이 구간 전체를 한 번에 잡을 수 있습니다.

### ❌ 114번 — 첫 성공까지의 시행 횟수

**Q.** 첫 번째 성공이 나올 때까지의 시행 횟수를 나타내는 분포는?
① 음이항분포 ② 초기하분포 ③ 기하분포 ④ 균등분포

| 내 답 | 정답 |
|---|---|
| ① 음이항분포 | **③ 기하분포** |

**해설(PART B)** — 첫 성공까지의 시행 횟수 = 기하분포.

---

### ❌ 115번 — 비복원 추출에서 성공 횟수의 분포

**Q.** 비복원 추출에서 성공 횟수의 분포를 나타내는 것은?
① 이항분포 ② 기하분포 ③ 포아송 분포 ④ 초기하분포

| 내 답 | 정답 |
|---|---|
| ② 기하분포 | **④ 초기하분포** |

**해설(PART B)** — 비복원 추출의 성공 횟수 = 초기하분포.

**개념 정리 — 이산분포 5형제**

| 분포 | 핵심 조건 | 세는 것 |
|---|---|---|
| **베르누이** | 1회 시행 | 성공/실패 |
| **이항분포** | **복원**추출, n회 반복 | n번 중 성공 횟수 |
| **기하분포** | 복원추출 | **첫 성공까지**의 시행 횟수 |
| **초기하분포** | **비복원**추출 | 성공 횟수 (모집단이 줄어듦) |
| **포아송분포** | 단위시간·공간 | 드물게 발생하는 사건 횟수(평균=분산) |

**암기팁** — **"기하분포는 '몇 번째에 처음 성공?', 초기하분포는 '비복원'"** — 이름의 **"초"**를 "초(超)기하 = 기하분포를 비복원으로 확장·초월한 버전"으로 연상하면, **"비복원"이라는 조건이 보이면 무조건 초기하분포**로 연결됩니다.

**실수 포인트 ⚠️ 114·115번이 서로 얽힌 이유** — 114번(기하분포가 정답)에 음이항분포를, 115번(초기하분포가 정답)에 기하분포를 쓰신 것은 **"이름이 비슷한 이산분포들을 하나의 덩어리로 뭉뚱그려 기억"**하고 계셨기 때문으로 보입니다. 위 표에서 **"무엇을 세는가"**만 정확히 구분하면 다섯 개가 자동으로 갈라집니다: 첫 성공까지 시행 횟수(기하) vs 비복원 성공 횟수(초기하).

---

### ❌ 116번 — 연속균등분포의 평균

**Q.** 구간 [a, b]에서 정의된 연속균등분포의 평균은?
① (a+b)/2 ② (b−a)/2 ③ ab/2 ④ (b−a)²/12

| 내 답 | 정답 |
|---|---|
| ② (b−a)/2 | **① (a+b)/2** |

**해설(PART B)** — 균등분포 U(a,b)의 평균 = (a+b)/2, 분산 = (b−a)²/12.

**암기팁** — **"평균은 두 끝을 더해서 반으로(중간점), 분산은 폭(b−a)의 제곱을 12로 나눔"** — 평균은 **구간의 정중앙**이라는 상식으로 접근하면 (a+b)/2가 자연스럽습니다. ④(b−a)²/12는 평균이 아니라 **분산** 공식입니다.

**실수 포인트** — ②(b−a)/2는 **"구간의 폭의 절반"**일 뿐, 구간의 위치(a가 어디서 시작하는지)를 반영하지 못합니다. 예를 들어 [10,20]과 [100,110]은 폭(10)은 같지만 평균(중간점)은 전혀 다릅니다 — **평균은 반드시 a와 b를 "더해야"** 나옵니다.

---

### ⚠️ 119번 — 무기억성을 갖는 연속분포 (헷갈림)

**Q.** '이미 경과한 시간이 앞으로 남은 대기시간에 영향을 주지 않는다'는 무기억성(Memoryless) 성질을 갖는 연속분포는?
① 정규분포 ② 균등분포 ③ 지수분포 ④ 카이제곱 분포

→ **정답 ③ 지수분포**

**해설(PART B)** — 지수분포가 무기억성을 갖는다(이산에서는 기하분포).

**암기팁** — **"지수분포 = 무기억성"**은 통계기법 파트 최빈출 짝입니다. **연속분포 중 유일하게 무기억성을 가지는 것이 지수분포**이고, 이산분포 중에서는 **기하분포**가 같은 성질(몇 번을 실패했든 다음 성공 확률은 항상 동일)을 가집니다. "얼마나 기다렸든 상관없다"는 표현이 나오면 지수분포(연속) 또는 기하분포(이산)입니다.

---

### ⚠️ 120번 — 이항분포의 정규근사 조건 (헷갈림)

**Q.** 이항분포를 정규분포로 근사할 수 있는 일반적인 조건은?
① np ≥ 5 이고 n(1−p) ≥ 5 ② n ≥ 10 ③ p ≥ 0.5 ④ np ≤ 5

→ **정답 ① np ≥ 5 이고 n(1−p) ≥ 5**

**해설(PART B)** — np ≥ 5, n(1−p) ≥ 5일 때 정규근사 가능.

**암기팁** — **"성공 기대횟수(np)와 실패 기대횟수(n(1−p)) 둘 다 5 이상"**이어야 종모양이 충분히 대칭적으로 갖춰집니다. **n이나 p 하나만으로는 판단할 수 없고, 반드시 두 조건을 "동시에" 만족**해야 합니다.

---

### ❌ 124번 — 카이제곱 분포의 특징

**Q.** 카이제곱 분포에 대한 설명으로 옳은 것은?
① 좌우 대칭이며 음수 값을 갖는다
② 항상 0 이상의 값을 가지며 오른쪽으로 치우쳐 있다
③ 평균이 항상 0이다
④ 자유도와 무관하다

| 내 답 | 정답 |
|---|---|
| ④ 자유도와 무관하다 | **② 항상 0 이상의 값을 가지며 오른쪽으로 치우쳐 있다** |

**해설(PART B)** — 카이제곱은 0 이상, 오른쪽으로 치우친 비대칭 분포.

**개념 정리 — 분포 모양 빠른 정리**

| 분포 | 값의 범위 | 대칭 여부 |
|---|---|---|
| **정규분포** | 전체 실수 | 좌우 대칭 |
| **t분포** | 전체 실수 | 좌우 대칭(꼬리 두꺼움) |
| **카이제곱 분포** | **0 이상** | **오른쪽으로 치우침**(비대칭) |
| **F분포** | 0 이상 | 오른쪽으로 치우침 |

**암기팁** — **카이제곱 분포는 "표준정규분포를 제곱해서 더한 값"**입니다. 제곱을 하니 **음수가 나올 수 없고(0 이상)**, 작은 값 근처에 몰려있다가 큰 값 쪽으로 길게 뻗는 **오른쪽 치우침(양의 왜도)** 모양이 됩니다.

**실수 포인트 ⚠️** — ④(자유도와 무관)는 정반대입니다. **카이제곱 분포는 자유도에 따라 모양이 달라지는 대표적인 분포**입니다(자유도가 커질수록 정규분포에 가까워짐). "무관하다"는 단정형 보기는 대부분 함정입니다.

---

## ⚠️ 오답 패턴 정리

### ① 3자 순환 오답 — 필터 → 래퍼 → 임베디드가 한 칸씩 밀림

22번(필터→래퍼로 오답), 23번(래퍼→임베디드로 오답), 24번(임베디드→차원축소로 오답)이 정확히 한 칸씩 밀려 있습니다.

> 💡 **"거르는 시점" 순서(모델 전=필터 → 모델 반복=래퍼 → 모델 내장=임베디드)를 문장으로 만들어 통째로 외우세요.** 세 이름의 순서 자체가 헷갈림의 원인이므로, 순서를 고정하는 것이 개별 암기보다 효과적입니다.

### ② 구간 전체 취약 — 차원 축소 기법 5개 미정리

34·38·39·40번, 이 소단원 문항이 전부 오답입니다. PCA·LDA는 익숙해서 다른 문제에도 오답으로 끌려나오고, MDS·ICA·SVD는 정의 자체가 헷갈리는 상태입니다.

> 💡 **PART 1-4의 총정리표 하나로 5개 기법을 한 번에 정리하세요.** 특히 MDS(Distance)·ICA(Independent)는 영문 풀네임에 정답 키워드가 그대로 들어 있습니다.

### ③ 변수 선택 vs 변수 추출(차원 축소) 경계를 넘나듦

24번(임베디드 문제에 차원축소를 답으로), 25번(래퍼 탐색법 문제에 PCA를 답 후보로 헷갈림) — **"있는 변수 중에서 고르기"(변수 선택)와 "변수를 조합해 새로 만들기"(차원 축소)를 같은 범주로 착각**하는 패턴이 반복됩니다.

> 💡 **변수 선택(필터·래퍼·임베디드) = 원래 변수 중 일부를 그대로 사용** / **차원 축소(PCA·LDA·SVD·MDS·ICA) = 변수를 조합해 새로운 축을 만듦.** 문제에 "선택"·"부분집합"이 있으면 앞의 3기법, "축"·"성분"·"사영"이 있으면 뒤의 5기법입니다.

### ④ 이름이 비슷한 통계 용어 쌍 혼동

핫덱↔콜드덱(14번), 로그 변환↔제곱 변환(43번), 비닝↔스무딩(45번)처럼 **비슷한 층위의 용어 두 개 중 하나를 잘못 골랐습니다.**

> 💡 이런 쌍은 각 용어의 **정의를 한 문장으로 대구(對句)를 맞춰** 외우면 효과적입니다 (예: "Hot=지금 내부 / Cold=예전 외부", "로그=압축 / 제곱=확대").

### ⑤ 목록 뼈대가 흔들리면 그 아래 세부 정의까지 연쇄로 흔들림

56번(EDA 창시자) · 57번(EDA 4대 주제 목록) · 58번(그중 '저항성'의 정의)이 연달아 틀렸습니다. **57번에서 4대 주제라는 "뼈대" 자체가 정리되지 않다 보니, 그 뼈대의 항목 하나(저항성)를 자세히 묻는 58번까지 함께 흔들린 것**입니다. 1과목의 "준비도 6영역"·"거버넌스 4체계"처럼, **목록형 개념은 뼈대(전체 항목)를 먼저 확정해야 개별 정의 문제도 안정적으로 풀립니다.**

> 💡 새 영역(EDA 등)에 처음 진입했을 때는 **개별 정의보다 "몇 개짜리 목록인가"부터 표로 그려 확정**하세요. 뼈대가 잡히면 이후 문항에서 그 뼈대를 반대로 묻든(57번), 항목 하나를 자세히 묻든(58번) 흔들리지 않습니다.

### ⑥ 정의를 정반대로 뒤집어 기억함

65번(왜도 방향에 따른 대푯값 순서 반대), 76번(히스토그램·막대그래프 정의 반대), 81번(공분산이 "방향을 모른다"고 반대로 이해), 100번(시각화 마지막 두 단계 순서 반대) — **네 문항 모두 정답과 정확히 반대 방향의 보기를 골랐습니다.**

> 💡 이런 "반대 방향" 오류는 **결과만 외우지 말고 "왜 그런가"라는 인과관계를 한 번 더 되짚으면** 방향이 저절로 잡힙니다 (예: "오른쪽 꼬리가 평균을 오른쪽으로 끌어당긴다" → 최빈<중앙<평균). 정답을 외우는 것보다 **원리를 한 문장으로 말할 수 있는지**를 스스로 확인하세요.

### ⑦ 이름이 비슷한 이산·연속분포 뭉뚱그림

114·115번(기하분포↔초기하분포), 119번(지수분포의 무기억성), 120번(정규근사 조건)까지 **111~120번 구간의 확률분포가 서로 섞인 상태**입니다.

> 💡 **PART 3-2의 이산분포 5형제 표 하나로 이 구간 전체가 정리됩니다.** "무엇을 세는가"(시행 횟수 vs 성공 횟수, 복원 vs 비복원)만 구분하면 이름이 자동으로 갈라집니다.

---

## 🎯 핵심 요약 카드

| # | 한 줄 정리 |
|---|---|
| 1 | **노이즈 = 무작위 측정 오차** (이상값=극단값과는 다른 개념) |
| 2 | **'다중' 대치법만 별도 범주** (단순은 완전분석·평균·단순확률) |
| 3 | 다중 대치법 순서 = **대치 → 분석 → 결합** (채워야 분석 가능) |
| 4 | **핫덱=현재 조사 내부 / 콜드덱=외부·과거 자료** |
| 5 | **필터=모델 전(통계척도) / 래퍼=모델 반복(성능평가) / 임베디드=모델 내장(학습과 동시)** |
| 6 | **PCA는 변수 선택이 아니라 변수 추출** — 래퍼 탐색법에 포함 안 됨 |
| 7 | 임베디드 예 = **라쏘·릿지·엘라스틱넷** (상관계수 필터링은 필터) |
| 8 | **라쏘=L1=계수를 0으로(변수선택 O) / 릿지=L2=축소만(변수선택 X)** |
| 9 | PCA는 **비지도**라 **종속변수 개념 없음** |
| 10 | **LDA만 지도**(클래스 간 분산↑ 클래스 내 분산↓), 나머지 4개는 비지도 |
| 11 | **MDS=거리(Distance) 보존 / ICA=독립(Independent) 성분 분리** |
| 12 | **SVD = 행렬을 3개로 분해**, 추천 시스템에 활용 |
| 13 | **양의 왜도(오른쪽 꼬리) → 로그 변환으로 압축** |
| 14 | **Box-Cox = 최적 λ를 찾는 로그 변환의 확장판** |
| 15 | **비닝 = 구간으로 나눠 범주화** (스무딩은 잡음 완화가 목적) |
| 16 | **로버스트 스케일링 = 중앙값·IQR** (이상값에 강함, 표준화는 약함) |
| 17 | **SMOTE = 최근접 이웃 보간으로 합성**(생성) / ENN·토멕링크 = 제거(정제) |
| 18 | **EDA 창시자 = 존 튜키** (칼피어슨=상관계수, 피셔=ANOVA, 고셋=t분포) |
| 19 | EDA 4대 주제 = **저·잔·재·현** (저항성·잔차해석·자료재표현·현시성), 인과성 ❌ |
| 20 | **저항성 = 이상값에 안 흔들림**(48번 로버스트 스케일링과 같은 아이디어) |
| 21 | **현시성 = 그래프로 보여줌**(한자 '示' = 보이다) |
| 22 | **기하평균=비율·성장률 / 조화평균=단위당 값(속도)** |
| 23 | **최빈값은 산포도가 아니라 대푯값** |
| 24 | **변동계수(CV)=표준편차÷평균**, 단위 다른 집단 비교용 |
| 25 | **첨도: 정규분포=3, 초과첨도=0** |
| 26 | **히스토그램=연속형(막대 붙음) / 막대그래프=범주형(막대 떨어짐)** |
| 27 | **박스플롯 상자 경계=Q1·Q3** (최댓값·최솟값 아님) |
| 28 | **Q-Q 플롯 = 이론분포와의 적합도** 시각 확인 |
| 29 | **공분산은 방향은 알려주되 크기 비교 불가**(단위의존) → 상관계수로 표준화 |
| 30 | **켄달의 타우 = 일치쌍·불일치쌍** 기반 순위상관 |
| 31 | **카토그램=지도 면적 자체 왜곡** / 등치지역도=색상만 |
| 32 | **시각화 절차 = 구조화 → 시각화 → 시각 표현** |
| 33 | **표본공간=모든 결과, 사건=부분집합** |
| 34 | **전확률정리=원인들→전체결과** / 베이즈정리=결과→원인(역추론) |
| 35 | **이산=PMF(질량), 연속=PDF(밀도)** |
| 36 | **기하분포=첫 성공까지 시행횟수** / **초기하분포=비복원 성공횟수** |
| 37 | **균등분포 평균=(a+b)/2**, 분산=(b−a)²/12 |
| 38 | **무기억성 = 지수분포**(연속) / 기하분포(이산) |
| 39 | **이항→정규근사 조건: np≥5 그리고 n(1−p)≥5** (둘 다 동시) |
| 40 | **카이제곱분포=0 이상·오른쪽 치우침**, 자유도 따라 모양 변함 |

---

## 📝 복습 체크리스트

| 문항 | D+1 | D+3 | D+7 | 졸업 |
|---|---|---|---|---|
| 5 | ☐ | ☐ | ☐ | ☐ |
| 10 | ☐ | ☐ | ☐ | ☐ |
| 13 | ☐ | ☐ | ☐ | ☐ |
| 14 | ☐ | ☐ | ☐ | ☐ |
| 22 · 23 · 24 | ☐ | ☐ | ☐ | ☐ |
| 25 | ☐ | ☐ | ☐ | ☐ |
| 29 | ☐ | ☐ | ☐ | ☐ |
| 30 | ☐ | ☐ | ☐ | ☐ |
| 34 | ☐ | ☐ | ☐ | ☐ |
| 38 · 39 · 40 | ☐ | ☐ | ☐ | ☐ |
| 43 | ☐ | ☐ | ☐ | ☐ |
| 44 | ☐ | ☐ | ☐ | ☐ |
| 45 | ☐ | ☐ | ☐ | ☐ |
| 48 | ☐ | ☐ | ☐ | ☐ |
| 54 | ☐ | ☐ | ☐ | ☐ |
| 56 · 57 · 58 | ☐ | ☐ | ☐ | ☐ |
| 59 | ☐ | ☐ | ☐ | ☐ |
| 63 · 64 | ☐ | ☐ | ☐ | ☐ |
| 65 | ☐ | ☐ | ☐ | ☐ |
| 67 | ☐ | ☐ | ☐ | ☐ |
| 68 | ☐ | ☐ | ☐ | ☐ |
| 69 | ☐ | ☐ | ☐ | ☐ |
| 72 | ☐ | ☐ | ☐ | ☐ |
| 76 | ☐ | ☐ | ☐ | ☐ |
| 77 | ☐ | ☐ | ☐ | ☐ |
| 80 | ☐ | ☐ | ☐ | ☐ |
| 81 | ☐ | ☐ | ☐ | ☐ |
| 84 | ☐ | ☐ | ☐ | ☐ |
| 85 | ☐ | ☐ | ☐ | ☐ |
| 95 | ☐ | ☐ | ☐ | ☐ |
| 100 | ☐ | ☐ | ☐ | ☐ |
| 101 | ☐ | ☐ | ☐ | ☐ |
| 106 | ☐ | ☐ | ☐ | ☐ |
| 107 | ☐ | ☐ | ☐ | ☐ |
| 114 · 115 | ☐ | ☐ | ☐ | ☐ |
| 116 | ☐ | ☐ | ☐ | ☐ |
| 119 | ☐ | ☐ | ☐ | ☐ |
| 120 | ☐ | ☐ | ☐ | ☐ |
| 124 | ☐ | ☐ | ☐ | ☐ |

---

## ✅ 남은 과제

1. **🔴 변수 선택 3기법(필터·래퍼·임베디드) 순서 고정** — "거르는 시점" 문장 하나로 22·23·24·29번이 동시에 풀립니다.
2. **🔴 차원 축소 5기법 총정리표 암기** — 34·38·39·40번이 표 하나로 해결됩니다. 영문 풀네임(Distance/Independent 등)으로 접근하세요.
3. **🔴 EDA 4대 주제 뼈대부터 확정** — "저·잔·재·현" 4글자만 확실히 외우면 56·57·58·59번이 동시에 안정됩니다.
4. **🔴 기하평균 ↔ 조화평균 짝 고정** — "%(비율)=기하, 단위/시간=조화" 한 문장으로 63·64번을 동시에 잡으세요.
5. **🔴 이산분포 5형제 표 암기** — "무엇을 세는가"만 구분하면 114·115·116·119·120번이 한 번에 정리됩니다.
6. **🟡 "상관계수 0 = 독립 아님" 재확인** — 85번은 이 시험 전체에서 가장 자주 나오는 함정입니다(PART C 최다 함정 2번). 81·84번과 묶어 상관분석 전체를 복습하세요.
7. **🟡 헷갈림 11문항 확신도 올리기** — 14·25·44·45·59·67·69·80·106·119·120번의 "못 박을 키워드"를 다시 확인하세요.
8. **다음 단계** — 125번부터 이어서 풀면 통계기법 나머지 구간(표본추출·추정·가설검정)이 채워집니다.

## 📖 부록 — PART C 시험장 직전 암기노트

> 2과목 예상문제 200제의 **PART C**를 오답노트에 옮겨 둔 것입니다. **시험 직전에는 이 부록만 15분 훑으면 됩니다.**

### 1️⃣ 데이터 전처리

**결측값 3유형**
| 유형 | 뜻 | 예시 |
|---|---|---|
| **MCAR** | 완전 무작위 | 설문지 일부가 물에 젖어 소실 |
| **MAR** | 관측된 다른 변수에 의존 | 남성이 우울증 문항에 덜 응답 |
| **NMAR** | 결측 변수 자체 값에 의존 | 고소득자가 소득을 숨김 |

- 처리: 삭제(완전분석법) / 단순 대치(평균·중앙값·최빈값·핫덱·회귀) / **다중 대치(대치→분석→결합)**
- ⚠️ **평균 대치 = 분산 과소 추정**

**이상값 검출**
- **ESD**: 평균 ± **3**σ
- **IQR**: Q1 − **1.5**×IQR ~ Q3 + **1.5**×IQR 밖
- 마할라노비스 거리, 박스플롯

**변수 선택 3기법**
| 기법 | 특징 | 예 |
|---|---|---|
| 필터 | 모델과 독립, 통계 척도 | 상관계수, 카이제곱 |
| 래퍼 | 모델 성능으로 평가 | **전진선택·후진제거·단계적** |
| 임베디드 | 학습에 내장 | **라쏘(L1)·릿지(L2)**·엘라스틱넷 |

> 💡 **L1 = 변수 선택 O (계수 0 가능) / L2 = 축소만**

**차원 축소**
| 기법 | 핵심 |
|---|---|
| PCA | **분산 최대화**, 비지도 |
| LDA | **클래스 분리 최대화**, 지도 |
| 요인분석 | 잠재 요인 규명 |
| SVD | 행렬 3분해 |
| MDS | 거리 기반 저차원 배치 |
| ICA | 독립 성분 분리 |

**스케일링**: Min-Max(0~1) / Z-score(평균0·표준편차1) / 로버스트(중앙값·IQR)
**불균형**: 언더샘플링(정보 손실) / 오버샘플링(과적합) / **SMOTE(최근접 이웃 보간 합성)** / 임계값 이동

---

### 2️⃣ EDA & 기초통계

- **EDA 4주제**: **저항성 · 잔차 해석 · 자료 재표현 · 현시성** (창시자: **존 튜키**)
- **중심**: 평균 / 중앙값 / 최빈값 · 기하평균(비율) · 조화평균(속도)
- **산포**: 범위 · IQR(Q3−Q1) · 분산 · 표준편차 · **변동계수 = 표준편차/평균**
- **표본분산은 n−1**로 나눔 (불편성 확보)

**왜도·첨도**
| 값 | 의미 |
|---|---|
| 왜도 > 0 | **오른쪽 꼬리 김** → 최빈 < 중앙 < 평균 |
| 왜도 < 0 | 왼쪽 꼬리 김 → 평균 < 중앙 < 최빈 |
| 첨도 = 3 | 정규분포 (초과첨도 0) |
| 첨도 > 3 | 뾰족·꼬리 두꺼움 |

**시각화 5분류**
| 분류 | 대표 그래프 |
|---|---|
| 시간 | 선, 막대, 누적 영역 |
| 분포 | 파이, 도넛, **트리맵** |
| 관계 | **산점도, 버블차트** |
| 비교 | **히트맵, 체르노프 페이스, 스타차트, 평행좌표** |
| 공간 | **등치지역도, 도트맵, 카토그램**, 버블맵 |

**상관분석**
- 피어슨(등간·비율, 선형) / **스피어만·켄달(순위)** / 카이제곱(범주형)
- **r = 0은 '선형관계 없음'이지 독립 아님**
- 공분산은 단위 의존 → 표준화한 것이 상관계수
- 다중공선성 진단 = **VIF**

---

### 3️⃣ 확률분포

| 분포 | 평균 | 분산 | 핵심 |
|---|---|---|---|
| 베르누이 | p | p(1−p) | 1회 시행 |
| **이항** | **np** | **np(1−p)** | n회 독립 시행 |
| **포아송** | **λ** | **λ** | 평균 = 분산 |
| 기하 | 1/p | — | 첫 성공까지 시행 |
| 초기하 | — | — | **비복원** 추출 |
| 균등 | (a+b)/2 | (b−a)²/12 | — |
| 정규 | μ | σ² | 좌우 대칭 |
| **지수** | 1/λ | — | **무기억성** |

- **카이제곱**: 표준정규 제곱합, 0 이상, 우측 치우침
- **t분포**: 좌우 대칭, 정규보다 꼬리 두꺼움, 자유도↑ → 정규 근사
- **F분포**: 두 분산의 비
- 정규근사 조건: **np ≥ 5 그리고 n(1−p) ≥ 5**
- E(aX+b) = aE(X)+b / **Var(aX+b) = a²Var(X)**

---

### 4️⃣ 표본추출 & 추정

**확률 표본추출**: 단순 무작위 · **계통**(간격 K) · **층화**(층 내 동질) · **군집**(집락 내 이질)
**비확률**: 편의 · 판단 · 할당 · 눈덩이

- **중심극한정리**: n이 크면 표본평균 분포 → 정규분포
- **표준오차 = σ/√n** → n이 4배면 SE는 **1/2**
- 표본비율 분산 = **p(1−p)/n**
- (n−1)S²/σ² ~ **χ²(n−1)**

**추정량 4대 성질**: **불·효·일·충** (불편성·효율성·일치성·충분성)
**추정 방법**: MLE(우도 최대) · 적률법 · 최소제곱법

**신뢰구간**
- σ 기지 → **Z** / σ 미지·소표본 → **t**
- 신뢰수준↑ → 구간 **넓어짐** / n↑ → 구간 **좁아짐**
- 사전 정보 없는 비율 → **p = 0.5** 사용
- ⚠️ 95% 신뢰구간은 "**절차의 장기 성공률**"이지 특정 구간의 확률이 아님

---

### 5️⃣ 가설검정

| 구분 | 내용 |
|---|---|
| H₀ | 차이 없음(기존 통념) |
| H₁ | 연구자가 입증하려는 주장 |
| **제1종 오류(α)** | 참인 H₀ 기각 |
| **제2종 오류(β)** | 거짓인 H₀ 채택 |
| 검정력 | **1 − β** |
| 판단 | **p < α → H₀ 기각** |

> ⚠️ **α↓ → β↑** (상충). 둘 다 줄이려면 **n을 늘려야** 함.

**검정 방법 매칭 (최빈출)**
| 상황 | 방법 |
|---|---|
| 모분산 기지·대표본, 평균 | **Z검정** |
| 한 집단 평균 vs 기준값 | 단일표본 t |
| 사전-사후 (동일 대상) | **대응표본 t** |
| 독립된 두 집단 평균 | **독립표본 t** |
| 두 집단 **분산** 비교 | **F검정** |
| 세 집단 이상 평균 | **ANOVA** (F = MSB/MSW) |
| 범주형 × 범주형 | **카이제곱** |

**카이제곱 3종**: 적합도(이론 분포 일치) / 독립성(1모집단 2변수) / 동질성(여러 모집단 분포 동일)
**ANOVA 가정**: **정규성·등분산성·독립성** → 사후검정: Tukey, Scheffe, Duncan

**모수 ↔ 비모수 대응**
| 모수 | 비모수 |
|---|---|
| 독립표본 t | **만-휘트니 U** |
| 대응표본 t | **윌콕슨 부호순위** |
| 일원배치 ANOVA | **크루스칼-왈리스** |
| 피어슨 상관 | **스피어만** |

**정규성 검정**: 샤피로-윌크, 콜모고로프-스미르노프, Q-Q 플롯

---

### ⚠️ 최다 함정 TOP 10

1. **평균 대치 → 분산 과소 추정** (과대 아님)
2. **상관계수 0 = 선형관계 없음** (독립 아님)
3. **t분포는 자유도가 작을수록 꼬리가 두꺼워짐**
4. **F검정은 분산 비교** (평균 아님)
5. **α를 낮추면 β는 커짐** (둘 다 감소 아님)
6. **95% 신뢰구간 ≠ 모수가 들어갈 확률 95%**
7. **PCA는 비지도, LDA는 지도**
8. **정규분포는 σ가 커지면 납작해짐** (뾰족 아님)
9. **크루스칼-왈리스는 비모수** (정규성 검정 아님)
10. **표준오차는 1/√n에 비례** (1/n 아님)

---

*끝. 시험 직전에는 PART C만 15분 훑으면 됩니다.*
